// SPDX-FileCopyrightText: 2026 Mattia Egloff <mattia.egloff@pm.me>
//
// SPDX-License-Identifier: GPL-3.0-or-later

// Minimal settings for CI Maven publishing only.
// This Gradle project exists solely to package pre-built JNI libs + Kotlin
// bindings into an AAR and publish to the core project's Maven registry.

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "vauchi-platform-publish"
include(":vauchi-platform")
