apiVersion: v1
kind: Service
metadata:
  name: {{ include "webbook-relay.fullname" . }}
  labels:
    {{- include "webbook-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: websocket
      protocol: TCP
      name: websocket
  selector:
    {{- include "webbook-relay.selectorLabels" . | nindent 4 }}
