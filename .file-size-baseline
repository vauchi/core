# SPDX-FileCopyrightText: 2026 Mattia Egloff <mattia.egloff@pm.me>
#
# SPDX-License-Identifier: GPL-3.0-or-later

# Generated 2026-03-15. Updated 2026-03-16 with exemptions from PRB-2026-032.
# Updated 2026-03-24: added vauchi-app entries after core boundary split.
# Files must not grow beyond baseline. EXEMPT files have structural justification.
# SPLIT files are queued for splitting in PRB-2026-032 implementation phases.

# SPLIT — Phase 1: zero-risk type/FFI splits
vauchi-cabi/src/lib.rs 1842
vauchi-platform/src/types.rs 1547

# EXEMPT — UniFFI facade: splitting breaks binding generation (trust/notes/preview/import/diagnostic-scanner exports added; +9 for Phase 1-3 re-export split + scan_qr alias 2026-04-19; +57 for ADR-044 MobileError struct-variant migration 2026-04-20; +1 for G6 MobileImportWarning export 2026-04-20; +7 for device-link orchestrator re-export + 2x #[deprecated] on legacy structs 2026-04-26; +17 for save_test_delivery_record helper supporting G3 ScreenModel API gaps 2026-04-27; +1 for G2 MobileLabelContactRow + MobileLabelContactStatus re-exports 2026-04-27; +5 for G4 mobile_contact_detail mod + re-exports 2026-04-27; +1 for Pair 5 platform_app_engine_device_link mod 2026-04-29; +2 for mobile_app_preferences mod + MobileAppPreferences re-export 2026-05-03)
vauchi-platform/src/lib.rs 2725

# EXEMPT — UniFFI facade for exchange protocol: command/event enums and
# their From impls must be co-located so binding generation produces
# matching iOS / Android variant shapes. Splitting would scatter the
# round-trip mappings.
# (+10 for ADR-031 file-picker mirror: FilePickedFromUser +
#  FilePickCancelledByUser variants + From arms 2026-05-03 Phase 1 of
#  2026-05-03-core-file-picker-command)
vauchi-platform/src/exchange.rs 1027

# EXEMPT — UniFFI facade + cycle-thread orchestrator: the audio-handshake
# protocol straddles `MobileMultiStageSession` (the UniFFI surface,
# can't split without breaking generated bindings) and the cycle-loop
# free functions (`try_autonomous_audio_trigger`, `cycle_loop`) that
# the surface dispatches into. Splitting would force a cross-module
# private API for the cycle-thread free fns the orchestrator + state
# machine share. (+15 for Phase 1.C polish: listener-presence gate in
# `try_autonomous_audio_trigger` so Glance flows never advance the
# inner state machine — 2026-05-11-hover-graduation-plan.)
# (+65 for Phase 1.C.7 wire-up of 2026-05-11-hover-graduation-plan:
#  `try_audio_timeout` helper fn + cycle-loop call site that polls
#  `MultiStageSession::check_and_apply_audio_timeout` to enforce
#  the audio listen-window budget when Listening wedges.)
vauchi-platform/src/multistage_exchange.rs 1080

# EXEMPT — state machine: scattered transitions create protocol bugs
# (+8 for ADR-031 audio-as-samples migration — encode at emit, accept
#  AudioSamplesRecorded at receive 2026-05-01)
# (+4 for ADR-031 file-picker ack-arms: FilePickedFromUser +
#  FilePickCancelledByUser handled identically to ImageReceived /
#  ImagePickCancelled — exchange session ignores them, vauchi-app
#  drives import 2026-05-03 Phase 1 of 2026-05-03-core-file-picker-command)
vauchi-core/src/exchange/session.rs 1814
# (+29 for Phase 1.C.3a foothold of
#  2026-05-11-hover-graduation-plan.md: audio_proximity field
#  + getter on MultiStageSession, AudioProximityState import.
#  Lives in core per the design-pass Option B so the session
#  (protocol producer) and the engine (renderer consumer)
#  share one enum.)
# (+83 for Phase 1.C.3b state machine: AudioStateError enum,
#  set_audio_proximity setter with transition validation,
#  audio_transition_allowed helper. Security gate — Confirmed
#  is reachable only from Listening. Retry path Failed→Listening
#  per G1.3.)
# (+20 for Phase 1.C.3e-iii: session_id() getter + doc explaining
#  the temporary use as audio-handshake challenge stand-in.)
# (+31 for Phase 1.C.3e-iv: peer_session_id() getter +
#  verify_audio_response with constant-time eq via subtle.
#  Crypto-adjacent comparison kept in core where subtle is a
#  dep, called from vauchi-platform's process_audio_samples_recorded.)
# (+79 for Phase 1.C.7 GREEN of 2026-05-11-hover-graduation-plan:
#  `audio_listening_started_at` field + `AUDIO_LISTEN_TIMEOUT` const
#  + `check_and_apply_audio_timeout` method enforcing the listen-
#  window budget. Defensive backstop per ADR-031 in case the audio
#  adapter is silent on timeout.)
vauchi-core/src/exchange/multistage/session.rs 1431

# EXEMPT — protocol command/event enum: variants + variant_name match
# + From impls + serde-discriminant ordering must be co-located, splitting
# scatters round-trip mappings (mirror of vauchi-platform/src/exchange.rs
# rationale at line 17). Tracks ADR-031 platform command/event
# additions append-only. Renamed 2026-05-04 from
# `vauchi-core/src/exchange/command.rs` to `vauchi-core/src/platform.rs`
# as part of Phase 0 of 2026-05-04-exchange-command-screen-presentation
# (audit `_private/docs/investigations/2026-05-04-exchange-command-naming-categorization.md`,
# ADR-031 Amendment A).
# (+111 for ADR-031 SetScreenBrightness + SetIdleTimerDisabled
#  variants + variant_name arms + 6 unit tests, Phase 1 of
#  2026-05-04-exchange-command-screen-presentation iOS retirement)
# (+4 for expanded module-level docs explaining the rename + the
#  three-axis boundary protocol vocabulary, 2026-05-04 Phase 0)
# (+37 for Command::SetOrientationLock + Orientation enum + variant_name
#  arm + 4 unit tests, Phase 2c of
#  2026-05-04-exchange-command-screen-presentation)
vauchi-core/src/platform.rs 1080

# EXEMPT — transport layer: all relay methods must be co-located for protocol coherence
# (+2 for #[tracing::instrument] on send_update + fetch — profiling, no logic change)
vauchi-core/src/network/http_transport.rs 1189

# EXEMPT — security-critical: crypto + verification belong together
vauchi-core/src/recovery/mod.rs 1253

# EXEMPT — migration ledger: coherence over size, version history must be co-located
# (+30 for V46 app_preferences singleton table — theme + language picks
# moving from Android SharedPreferences into core storage, problem
# 2026-05-01-android-humble-ui-deep-retirement Phase 2a/A3a)
vauchi-core/src/storage/migration.rs 1842

# EXEMPT — rekey ledger: all encrypted columns must be co-located for correctness (audit C1)
vauchi-core/src/storage/rekey.rs 1390

# SPLIT — Phase 3: extract connection management
vauchi-core/src/storage/mod.rs 1079

# SPLIT DONE — Phase 3: app_engine.rs (1437) split into mod.rs + routing.rs + screens.rs
# (+1 each on mod.rs + routing.rs for #[tracing::instrument] on handle_action + handle_hardware_event)
# (+32 on screens.rs for load_retry_entries + DeliveryItem.message_id wiring 2026-04-28
#  Pure Humble UI retirement Pair 1)
# (+18 on screens.rs for GroupDetail field_visibility wiring 2026-04-28 Pair 2)
# (+16 on routing.rs for ActionResult::SetGroupFieldVisibility handler 2026-04-28 Pair 2)
# (+3 on mod.rs for intercept_recovery_trust_toggle wiring 2026-04-28 Pair 3)
# (+7 on screens.rs for is_verified/fingerprint/recovery_trusted plumbing 2026-04-28 Pair 3)
# (+10 on mod.rs for AppScreen::MultiStageExchange variant + screen_id/from_screen_id 2026-04-28 Pair 4)
# (+3 on routing.rs for handle_hardware_event allowlist entry 2026-04-28 Pair 4)
# (+10 on screens.rs for AppScreen::MultiStageExchange engine factory 2026-04-28 Pair 4)
# (+60 on mod.rs for apply_multi_stage_{state,qr_payload,finalized,session_ended} bridge convenience methods 2026-04-28 Pair 4)
# (+31 on routing.rs for ActionResult::RetryFailedDeliveries handler 2026-04-28 Pair 1 retry-routing fix)
# (+10 on routing.rs for ActionResult::StartMultiStageExchange handler 2026-04-28 Pair 4 L8)
# (+77 on mod.rs for Round 2 audit P2-D — set_network_online + apply_offline_overlay
#   + ACTION_OFFLINE_BANNER + boot decision comment 2026-04-28
#   feature/round2-core-lifecycle-surface)
# (+7 on routing.rs for create_identity_with_onboarding atomic switch comment
#   2026-04-28 P1 §2.5)
# (+35 on screens.rs for theme + language SettingsConfig population from
#   bundled_themes() + i18n::get_available_locales() + AppPreferences,
#   problem 2026-05-01-android-humble-ui-deep-retirement Phase 2a/A3a)
# (+10 on screens.rs for AppScreen::DecoyContacts engine factory,
#   problem 2026-05-01-android-humble-ui-deep-retirement Phase 2c)
# (+4 on mod.rs for AppScreen::ChangePassword variant + screen_id arms,
#   feat/2026-05-10-change-app-password)
# (+16 on mod.rs for Phase 1.E.3 of 2026-05-11-hover-graduation-plan:
#   AppScreen::MultiStageExchange gains `{ mode: ExchangeMode }` payload —
#   variant docstring expanded, screen_id arm widened to `{ .. }`,
#   from_screen_id arm dropped with parameterized-screen comment.)
vauchi-app/src/ui/app_engine/mod.rs 1284
# (+78 on routing.rs for ADR-031 file-picker handler: pre-screen-guard
#  branch + handle_file_picked + format_import_toast helper, Phase 2A
#  of 2026-05-03-core-file-picker-command. Wires More-screen contacts
#  import; Onboarding/backup-restore deferred to Phase 2B.)
# (+117 on routing.rs for Phase 2B of 2026-05-03-core-file-picker-command:
#  AppScreen::Onboarding + AppScreen::DeviceReplacement arms in
#  handle_file_picked, BackupPasswordEntry detection in handle_completion,
#  execute_backup_restore helper that calls Vauchi::import_full_backup
#  and routes to MainScreen on success / LinkChoice with alert on failure.)
# (+19 on routing.rs for AppScreen::ChangePassword arm in handle_completion
#   — calls vauchi.change_app_password and surfaces ShowAlert on failure,
#   feat/2026-05-10-change-app-password)
# (+1 on routing.rs for slice 5 hoist of `let now = self.vauchi.clock().unix_seconds()`
#   before the FormDialog EditField match, threaded into the new
#   ContactCard::update_field_value(field_id, value, now) signature.)
# (+5 on routing.rs for Phase 1.E.3 of 2026-05-11-hover-graduation-plan:
#   destructure `mode` from StartMultiStageExchange and pass through to
#   AppScreen::MultiStageExchange { mode } + expanded comment block.)
vauchi-app/src/ui/app_engine/routing.rs 1267
# (+13 on screens.rs for Phase 1.E.3 of 2026-05-11-hover-graduation-plan:
#   AppScreen::MultiStageExchange { mode } match arm dispatches to
#   new_hover() / new_glance() with explanatory comment block.)
vauchi-app/src/ui/app_engine/screens.rs 1366

# SPLIT — Phase 3: extract into crypto, storage, verification files
vauchi-core/src/api/shred.rs 1081

# SPLIT — Phase 2: split into 5 files by subsystem
vauchi-core/tests/it/app_engine_tests.rs 2275

# SPLIT — Phase 2: split into 5 files by field type
vauchi-core/tests/it/field_validation_tests.rs 2256

# Grandfathered — below split threshold, monitor
# (+17 lines for is_active_engine_multi_stage_hover regression-gate
#  pair, Phase 1.C polish of 2026-05-11-hover-graduation-plan: pins the
#  helper that PAE consults to decide audio-listener registration.)
# (+19 for Phase 1.E.3 of 2026-05-11-hover-graduation-plan: 8 inline
#  `AppScreen::MultiStageExchange { mode: ExchangeMode::Glance }`
#  conversions + expanded comment in the navigate-away-and-back test
#  swapping its mode pick from Hover to Broadcast.)
# (+4 for stale-comment heal on
#  `is_active_engine_multi_stage_hover_returns_false_for_glance_multistage`
#  — reframes "pending the Phase 1.E mode-dispatcher" to the
#  post-1.E.3 shipped state.)
vauchi-core/tests/it/app_engine_navigation_tests.rs 1240
vauchi-core/tests/it/onboarding_tests.rs 1301
vauchi-core/tests/it/sync_device_orchestrator_tests.rs 1286
vauchi-core/tests/it/visibility_label_tests.rs 1220
vauchi-core/tests/it/migration_tests.rs 1226

# vauchi-app — extracted from vauchi-core, inherits exemption
# (+150 for G4 visibility helpers + ReciprocityBannerKind + inline tests
# supporting ScreenModel API gaps 2026-04-27)
# (+105 for is_verified/fingerprint/is_recovery_trusted fields + recovery
# permissions SettingsGroup + verify_button gating 2026-04-28 Pair 3)
# (+7 for Glance → MultiStageExchange handoff branch + test rewrite 2026-04-28 Pair 4 L8)
vauchi-app/src/ui/exchange.rs 3221
vauchi-app/src/ui/contact_detail.rs 1423

# EXEMPT — BLE exchange sub-flow: inline tests access private state (proximity_runner, shake_envelope_sent)
vauchi-app/src/ui/exchange_ble.rs 1057

# EXEMPT — state machine: 9 ProtocolState variants + camera-gate transitions need cohesion;
# inline tests access private state (state, peer_name, session_ended, camera_gate, scan_quality_tracker).
# Pair 4 of `_private/docs/problems/2026-04-28-pure-humble-ui-retire-native-screens` (2026-04-28).
# (+10 for as_any_mut impl supporting AppEngine downcasting bridge 2026-04-28 Pair 4)
# (+7 for pub PEER_SCAN_COMPONENT_ID re-export + doc comment so the
#  platform layer can route TextChanged from the QR scanner Component
#  into the cycle-thread session — Pair 4 frontend wiring 2026-04-28)
# (+12 for Command::SetOrientationLock dispatch in screen_entered/exited
#  + updated inline tests, Phase 2c of
#  2026-05-04-exchange-command-screen-presentation)
# (+100 for Phase 1.A of 2026-05-11-hover-graduation-plan.md:
#  AudioProximityState enum + audio_proximity engine field +
#  new_glance/new_hover mode-aware constructors + new() shim +
#  audio_proximity() getter + 2 inline tests pinning the
#  constructor invariants. Engine state-machine surface; per
#  file-size.md structural exemption — scattered transitions
#  create protocol bugs.)
# (+30 for Phase 1.B of 2026-05-11-hover-graduation-plan.md:
#  screen_entered appends Command::SwitchCamera + matching
#  glance/hover inline test pair replacing the prior single
#  screen_entered_emits_brightness… test. Defensive: removes
#  the implicit-coincidence dependency where engine and
#  consumer both default to back-camera.)
# (+245 for Phase 1.C.2 + 1.D engine-side rendering
#  (2026-05-11-hover-graduation-plan.md):
#  set_audio_proximity setter, build_screen audio-Failed
#  priority dispatch, build_audio_failed_screen with
#  distinct chrome (G1.3), build_status_indicator audio
#  narration layering (Listening/Confirmed), 7 inline tests
#  covering setter / cancelled-respect / status narration /
#  audio-failed precedence. Option-agnostic — engine-side
#  surface common to A/B/C; session-side 1.C.3+ commits to
#  Option B per the design pass.)
vauchi-app/src/ui/multi_stage_exchange.rs 1633

# EXEMPT — UniFFI facade growth: PlatformAppEngine wraps the full
# AppEngine surface JSON-side and now also owns the Pair 4
# auto-managed multi-stage session lifecycle (creates / cancels
# MobileMultiStageSession on screen transitions in/out of
# MultiStageExchange, routes QrScanned hardware events to
# session.process_scanned_qr, restarts on retry). Splitting would
# fragment the lifecycle hooks across multiple files.
# Pair 4 of `_private/docs/problems/2026-04-28-pure-humble-ui-retire-native-screens` (2026-04-28).
# (+140 for Round 2 audit core surface: boot(), biometric_unlock_check(),
# set_network_online() / is_network_online(), periodic_sync_tick() +
# periodic_sync_interval_seconds() + periodic_sync_max_retries() —
# `2026-04-28-lifecycle-session-residue-umbrella` items P1 / P2-B / P2-C
# / P2-D 2026-04-28 feature/round2-core-lifecycle-surface)
# Bumped 2026-04-28 to 1723 by Phases B2 + B3 of
# `2026-04-28-collapse-vauchi-platform-into-app-engine`. B2 added the
# 9 recovery-domain wrappers (+412); B3 adds the 4 emergency-broadcast
# wrappers (configure / send / get_config / disable, +95).
# Bumped 2026-04-28 to 1984 by Phase B4 — post-orchestrator
# device-linking wrappers (get_devices, device_count, is_primary_device,
# unlink_device, generate/parse_device_link_qr,
# create_device_link_session_initiator, +261). The 5 pre-orchestrator
# legacy methods are intentionally NOT migrated (superseded by the
# orchestrator session in
# `done/2026-04-27-device-link-orchestrator-phase2d-windows`).
# Bumped 2026-04-28 to 2073 by Phase B7 skeleton — adds
# `dispatch_domain_command(DomainCommand) -> DomainCommandResult` plus
# the first batch of variants (Consent: 5 commands).
# Bumped 2026-04-28 to 2235 by Phase B7 batch 2 — Content Updates (4
# commands: IsContentUpdatesSupported, CheckContentUpdates,
# ApplyContentUpdates, ReloadSocialNetworks; +162 incl. duplicated
# feature-gated impl that mirrors mobile_content.rs and will become
# the only copy after D3).
# Splitting is not an option (UniFFI facade — see
# `.claude/rules/file-size.md` Structural Exemptions).
# (+29 for Pair 4 peer_scan TextChanged auto-route mirroring the
#  existing QrScanned hardware-event auto-route — feeds the
#  cycle-thread session when the QR-scanner Component fires from
#  the multi-stage screen 2026-04-28)
# Bumped 2026-04-29 to 2485 by Phase B7 batch 5 — Aha Moments +
# Demo Contact (14 commands; backed by `.aha_moments` +
# `.demo_contact` JSON files mirroring the legacy VauchiPlatform
# layout; +221 lines split between dispatch arms and 6
# file-helper methods).
# Bumped 2026-04-29 to 2826 by Phase B7 batch 10 — Contact Card +
# CRUD (19 commands: GetOwnCard, AddField, UpdateField, RemoveField,
# SetDisplayName, SetOwnAvatar, ClearOwnAvatar, ListContacts,
# GetContact, SearchContacts, ContactCount, RemoveContact,
# Soft/Undo/Hard delete imported, Archive/Unarchive/ListArchived,
# Hide/Unhide; +341 lines).
# Bumped 2026-04-29 to 2931 by Phase B7 batch 3 — GDPR / Deletion
# (5 commands: ExportGdprData, ScheduleIdentityDeletion,
# CancelIdentityDeletion, ExecuteIdentityDeletion, GetDeletionState)
# + read-only ShredStatus. The 5 keychain-bound shred methods
# (panic_shred / soft_shred / hard_shred / cancel_shred /
# verify_shred) are deferred to a separate batch that adds
# MobilePlatformKeychain plumbing to PlatformAppEngine.
# Bumped 2026-04-29 to 3029 by Phase B7 batch 4 — Recovery leftovers
# (3 commands: VerifyRecoveryProof, UploadGuardianEntries,
# SaveRecoveryResponse). Completes the recovery domain (B2 covered
# the 9 main typed methods).
# Bumped 2026-04-29 to 3409 by Phase B7 batch 6 — Visibility Labels
# + Field Visibility (15 commands; +381 lines of dispatch).
# Bumped 2026-04-29 to 3575 by Phase B7 batch 7 — Passcode + Duress
# + Decoy (11 commands: SetupAppPassword, SetupDuressPassword,
# Authenticate, IsPasswordEnabled, IsDuressEnabled, DisableDuress,
# ConfigureDuressAlerts, GetDuressSettings, AddDecoyContact,
# ListDecoyContacts, DeleteDecoyContact; +166 lines).
# Bumped 2026-04-29 to 3890 by Phase B7 batch 8 — Delivery records,
# retry queue, offline queue counts (21 storage-only commands;
# +315 lines of dispatch arms). State-heavy methods (sync,
# get_sync_status, delivery-receipts / suppress-presence flags,
# backup export/import) deferred to a separate orchestration batch.
# Bumped 2026-04-29 to 3966 by Phase B7 batch 9 — Identity reads
# (CreateIdentity, GetPublicId, GetDisplayName, GetOwnFingerprint)
# + Onboarding helpers (DisplayNameSuggestions, ResetOnboarding).
# 6 commands; +76 lines of dispatch arms.
# Bumped 2026-04-29 to 4253 by Phase B7 batch 11 — Contact
# Verification + Duplicates + Notes + Field Notes + Nicknames +
# Custom Avatars + Social Search + Hidden Contacts + Footer Action
# Id (19 commands; +287 lines of dispatch arms; reuses pub(crate)
# enrich_contact / enrich_contacts_batch helpers promoted in the
# same MR).
# Bumped 2026-04-29 to 4346 by Phase B7 batch 12 — Backup + Import
# (5 commands: ExportBackup, ImportBackup, ExportFullBackup,
# ImportFullBackup, ImportContactsFromVcf; +93 lines of dispatch
# arms with hex⇄base64 transport encoding mirroring legacy
# mobile_delivery + mobile_import).
# Bumped 2026-04-29 to 4347 by Phase B7 batch 13 — Offline queue +
# decoy CRUD + counts (note: variants are duplicates of batch 7/8 —
# kept here only for the test additions; dispatch arms removed in
# combine merge).
# Bumped 2026-04-29 to 4410 by Phase B7 batch 14 — Search + display
# prefs + merge (4 commands: SetDisplayNamePreference,
# SetAvatarPreference, MergeContacts; SearchContacts variant
# de-duplicated against batch 10).
# Bumped 2026-04-29 to 4410 by Phase B7 batch 15 — Field visibility
# (variants and dispatch arms duplicate batch 6; only the test
# additions are kept in the combine merge).
# Bumped 2026-04-29 to 4482 by Phase B7 batch 16 — Onboarding state
# ops (5 commands: GetOnboardingProgress, CurrentOnboardingStep,
# IsOnboardingComplete, AdvanceOnboarding, SkipOnboardingStep; +72
# lines).
# Bumped 2026-04-29 to 4551 by Phase B7 batch 17 — Display options
# + paginated contact lists (2 commands: GetContactDisplayOptions,
# ListContactsPaginated; ListArchivedContacts already migrated by
# batch 10; +69 lines).
# Bumped 2026-04-29 to 4646 by Phase B7 batch 18 — Sync flag
# persistence (4 commands: IsDeliveryReceiptsEnabled,
# SetDeliveryReceiptsEnabled, IsSuppressPresenceEnabled,
# SetSuppressPresenceEnabled). Adds a `SyncFlags` JSON sidecar at
# `.sync_flags` so the legacy mutex-only flags now survive
# restarts; +95 lines incl. the sidecar helpers + struct.
# Bumped 2026-04-29 to 4753 by Phase B7 batch 19 — Contact detail
# view state + list social networks (2 commands:
# ContactDetailViewState, ListSocialNetworks; +107 lines incl. the
# badges/banners/actions builder mirroring mobile_contact_detail).
# Bumped 2026-04-29 to 4760 by Phase B7 batch 20 — Multipart QR
# encoding (1 command: EncodeMultipartQr; stateless wrapper around
# multipart_qr::encode_multipart; +7 lines).
# Bumped 2026-04-29 to 4797 by Phase B7 batch 21 — Certificate
# pinning persistence (2 commands: SetPinnedCertificate,
# IsCertificatePinningEnabled). Persists the PEM to a `.cert_pin`
# sidecar file so the choice survives restarts; +37 lines incl.
# the file-existence-as-state pattern + path helper.
# Bumped 2026-04-29 to 4836 by Pair 5 device-link session lifecycle
# (Pair 5 of 2026-04-28-pure-humble-ui-retire-native-screens).
# Adds the `device_link_session` field, three pub(crate) accessors,
# and the `after_screen_transition` + `dispatch_device_link_side_effects`
# wiring. The bulk (bridge listener + ensure/cancel/dispatch + test
# helpers, +328 lines) lives in the new sibling
# `platform_app_engine_device_link.rs` to keep this file lean.
# Bumped 2026-05-01 to 4945 by Phase 1.6 link-mode responder session
# lifecycle (`2026-04-27-deep-link-responder-flow`). Adds the
# `link_responder_session` field, the lazy `ensure_link_responder_session`
# private helper + idempotent `cancel_link_responder_session`, the
# `after_screen_transition` cancel-on-leave wiring, and the public
# UniFFI `current_link_responder_session()` getter (+109 lines). No
# bridge listener lives here — frontends own `on_commands`,
# `on_finalized`, `on_failed` dispatch, attached to the session
# returned by the getter.
# (+20 lines for cancel-session-first guard in `apply_multi_stage_*_for_test`
#  bridges — fixes flaky `apply_multi_stage_state_finalized_with_session_ended`
#  caused by cycle-thread racing the test's manual state pushes.)
# (+4 on top of main's 5099 baseline for the unlink_device cascade
#   from slice 4: revoke_device call now needs `now`.)
vauchi-platform/src/platform_app_engine.rs 5103

# EXEMPT — B7 dispatch test bundle: each batch ships with cohesive
# tests that share the create_engine_with_identity / drive_onboarding
# helpers; splitting per-batch would force duplicating those helpers
# across files and complicates the read-after-write smoke pattern.
# Will be split into per-domain test files only after B7 phase completes.
vauchi-platform/tests/it/platform_app_engine_domain_command_tests.rs 3251

# EXEMPT — HTTP transport: relay API surface (sync, OHTTP, pinning, exchange) is cohesive
# (moved up — guardian CRUD methods added for social recovery)
