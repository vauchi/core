{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ActionResult",
  "description": "The result of handling a user action.",
  "oneOf": [
    {
      "type": "string",
      "enum": [
        "Complete"
      ]
    },
    {
      "type": "object",
      "required": [
        "UpdateScreen"
      ],
      "properties": {
        "UpdateScreen": {
          "$ref": "#/definitions/ScreenModel"
        }
      },
      "additionalProperties": false
    },
    {
      "type": "object",
      "required": [
        "NavigateTo"
      ],
      "properties": {
        "NavigateTo": {
          "$ref": "#/definitions/ScreenModel"
        }
      },
      "additionalProperties": false
    },
    {
      "description": "The back gesture reached a back-stopping root: there is no screen to pop, so the frontend performs the platform's native back default (Android `moveTaskToBack` / minimize; iOS suspend / no-op; desktop no-op). Core owns the \"nothing to pop\" decision; the frontend owns only the native mechanism. Retires the frontend `can_go_back` gate on its system-back handler (ADR-044 Amendment 2a).",
      "type": "string",
      "enum": [
        "PerformNativeBack"
      ]
    },
    {
      "type": "object",
      "required": [
        "ValidationError"
      ],
      "properties": {
        "ValidationError": {
          "type": "object",
          "required": [
            "component_id",
            "message"
          ],
          "properties": {
            "component_id": {
              "type": "string"
            },
            "message": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Onboarding complete — navigate to the chosen destination.",
      "type": "object",
      "required": [
        "CompleteWith"
      ],
      "properties": {
        "CompleteWith": {
          "type": "object",
          "required": [
            "destination"
          ],
          "properties": {
            "destination": {
              "$ref": "#/definitions/PostOnboardingDestination"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Onboarding is finished; the engine has already navigated to the chosen post-onboarding screen. Frontends should flip their app state from \"onboarding\" to \"ready\" and render the current screen. Replaces frontend-side enumeration of onboarding `screen_id`s (`2026-07-06-mobile-domain-shell-violations` I7/A13).",
      "type": "object",
      "required": [
        "OnboardingComplete"
      ],
      "properties": {
        "OnboardingComplete": {
          "type": "object",
          "required": [
            "destination"
          ],
          "properties": {
            "destination": {
              "$ref": "#/definitions/PostOnboardingDestination"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Frontend should switch to the device linking flow in the given role.",
      "type": "object",
      "required": [
        "StartDeviceLink"
      ],
      "properties": {
        "StartDeviceLink": {
          "type": "object",
          "required": [
            "role"
          ],
          "properties": {
            "role": {
              "$ref": "#/definitions/DeviceLinkRole"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Frontend should open the contact detail view.",
      "type": "object",
      "required": [
        "OpenContact"
      ],
      "properties": {
        "OpenContact": {
          "type": "object",
          "required": [
            "contact_id"
          ],
          "properties": {
            "contact_id": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "App layer should perform a per-row mutation on a contact (archive/hide/delete and their inverses). The app layer maps this to the appropriate `Vauchi` call, then shows a toast with an undo action pointing to the inverse [`ContactActionKind`].",
      "type": "object",
      "required": [
        "ContactAction"
      ],
      "properties": {
        "ContactAction": {
          "type": "object",
          "required": [
            "contact_id",
            "kind"
          ],
          "properties": {
            "contact_id": {
              "type": "string"
            },
            "kind": {
              "$ref": "#/definitions/ContactActionKind"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Frontend should open the contact edit view.",
      "type": "object",
      "required": [
        "EditContact"
      ],
      "properties": {
        "EditContact": {
          "type": "object",
          "required": [
            "contact_id"
          ],
          "properties": {
            "contact_id": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Frontend should open the given URL in an external browser.",
      "type": "object",
      "required": [
        "OpenUrl"
      ],
      "properties": {
        "OpenUrl": {
          "type": "object",
          "required": [
            "url"
          ],
          "properties": {
            "url": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Frontend should display an alert dialog.",
      "type": "object",
      "required": [
        "ShowAlert"
      ],
      "properties": {
        "ShowAlert": {
          "type": "object",
          "required": [
            "message",
            "title"
          ],
          "properties": {
            "message": {
              "type": "string"
            },
            "title": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Frontend should open the camera for QR scanning.\n\nDeprecated per ADR-022 Addendum D: use `Commands` with `Command::QrRequestScan` instead. Will be removed after all frontends adopt the command/event protocol.",
      "type": "string",
      "enum": [
        "RequestCamera"
      ]
    },
    {
      "description": "Frontend should open the entry detail view for a MyInfo field.",
      "type": "object",
      "required": [
        "OpenEntryDetail"
      ],
      "properties": {
        "OpenEntryDetail": {
          "type": "object",
          "required": [
            "field_id"
          ],
          "properties": {
            "field_id": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Frontend should display a non-blocking toast. Does not replace the current screen.",
      "type": "object",
      "required": [
        "ShowToast"
      ],
      "properties": {
        "ShowToast": {
          "type": "object",
          "required": [
            "message"
          ],
          "properties": {
            "message": {
              "type": "string"
            },
            "undo_action_id": {
              "type": [
                "string",
                "null"
              ]
            },
            "undo_label": {
              "description": "Core-resolved label for `undo_action_id`. Both fields are present for an actionable toast and absent for an informational toast.",
              "type": [
                "string",
                "null"
              ]
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Backup export completed — frontend should save or share the data.\n\nThe data is a hex-encoded encrypted backup blob. The frontend presents a file-save dialog or share sheet.",
      "type": "object",
      "required": [
        "BackupExportComplete"
      ],
      "properties": {
        "BackupExportComplete": {
          "type": "object",
          "required": [
            "data"
          ],
          "properties": {
            "data": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "GDPR data export completed — frontend should save or share the JSON (file dialog / share sheet). Mirrors `BackupExportComplete`; the payload is the serialized `export_all_data` result.",
      "type": "object",
      "required": [
        "GdprExportComplete"
      ],
      "properties": {
        "GdprExportComplete": {
          "type": "object",
          "required": [
            "json"
          ],
          "properties": {
            "json": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "All data has been wiped — frontend should reset to initial state.",
      "type": "string",
      "enum": [
        "WipeComplete"
      ]
    },
    {
      "description": "App layer should start the device-link join (responder) machine with the given device name. Emitted by `DeviceLinkJoinEngine` when the user confirms the name. Core builds the responder, posts the request on the next poll tick, and advances through confirmation → adoption.",
      "type": "object",
      "required": [
        "DeviceLinkJoinStart"
      ],
      "properties": {
        "DeviceLinkJoinStart": {
          "type": "object",
          "required": [
            "device_name"
          ],
          "properties": {
            "device_name": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Frontend should perform hardware actions for the exchange protocol (ADR-031). Each command maps to a platform-specific operation (QR display, BLE scan, etc.). Unsupported commands should be answered with `Event::HardwareUnavailable`.",
      "type": "object",
      "required": [
        "Commands"
      ],
      "properties": {
        "Commands": {
          "type": "object",
          "required": [
            "commands"
          ],
          "properties": {
            "commands": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Command"
              }
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "App layer should navigate to MyInfo in preview mode for this contact.",
      "type": "object",
      "required": [
        "PreviewAs"
      ],
      "properties": {
        "PreviewAs": {
          "type": "object",
          "required": [
            "contact_id"
          ],
          "properties": {
            "contact_id": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "App layer should navigate to the Contacts screen (contact picker).",
      "type": "string",
      "enum": [
        "ShowContactPicker"
      ]
    },
    {
      "description": "App layer should navigate to the fingerprint verification screen.",
      "type": "object",
      "required": [
        "VerifyFingerprint"
      ],
      "properties": {
        "VerifyFingerprint": {
          "type": "object",
          "required": [
            "contact_id"
          ],
          "properties": {
            "contact_id": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "App layer should open a form dialog (create group, rename, etc.).",
      "type": "object",
      "required": [
        "ShowFormDialog"
      ],
      "properties": {
        "ShowFormDialog": {
          "type": "object",
          "required": [
            "dialog_type"
          ],
          "properties": {
            "context_id": {
              "type": [
                "string",
                "null"
              ]
            },
            "dialog_type": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "The app engine produced OS notifications that should be rendered. These are non-blocking and do not change the current screen.",
      "type": "object",
      "required": [
        "Notify"
      ],
      "properties": {
        "Notify": {
          "type": "object",
          "required": [
            "notifications"
          ],
          "properties": {
            "notifications": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PendingNotification"
              }
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "Frontend should display a help info overlay. Rendered as bottom sheet (mobile), popover (desktop), or inline text (TUI/CLI).",
      "type": "object",
      "required": [
        "ShowInfoOverlay"
      ],
      "properties": {
        "ShowInfoOverlay": {
          "type": "object",
          "required": [
            "body",
            "title"
          ],
          "properties": {
            "body": {
              "type": "string"
            },
            "title": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "App layer should persist a per-field visibility change for a group (a.k.a. visibility label). Emitted by `GroupDetailEngine` when the user toggles a field in the visibility list. AppEngine routing applies the change via `vauchi.set_group_field_visibility_and_repropagate` and re-emits the screen so any downstream count (Visible Fields) refreshes.",
      "type": "object",
      "required": [
        "SetGroupFieldVisibility"
      ],
      "properties": {
        "SetGroupFieldVisibility": {
          "type": "object",
          "required": [
            "field_id",
            "group_id",
            "visible"
          ],
          "properties": {
            "field_id": {
              "type": "string"
            },
            "group_id": {
              "type": "string"
            },
            "visible": {
              "type": "boolean"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "App layer should requeue every failed delivery in the list for an immediate retry attempt. Emitted by `DeliveryStatusEngine` when the user taps the \"Retry Failed\" footer button. AppEngine routing iterates the message ids and calls `vauchi.storage().retries().update_retry_next_time(id, now)` for each (mirror of `mobile_delivery::manual_retry`), then emits a `ShowToast` with the count of rescheduled messages.",
      "type": "object",
      "required": [
        "RetryFailedDeliveries"
      ],
      "properties": {
        "RetryFailedDeliveries": {
          "type": "object",
          "required": [
            "message_ids"
          ],
          "properties": {
            "message_ids": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "App layer should navigate to the multi-stage face-to-face exchange screen (`AppScreen::MultiStageExchange`).\n\nEmitted by `ExchangeEngine` when the user picks a mode whose implementation is the new core-driven multi-stage protocol. Pair 4 of `2026-04-28-pure-humble-ui-retire-native-screens` graduated `ExchangeMode::Glance`; Phase 1.E of `2026-05-11-hover-graduation-plan.md` extended the handoff to `ExchangeMode::Hover`. The `mode` payload tells AppEngine which engine constructor to use (`MultiStageExchangeEngine::new_hover()` vs `::new_glance()`) — Hover defaults to the front camera and runs the autonomous audio-handshake trigger; Glance stays back-camera + audio-quiet.\n\nFrontends never see this — `PlatformAppEngine` routes it to the dedicated screen which then auto-creates the `MobileMultiStageSession` and binds the bridge listener.",
      "type": "object",
      "required": [
        "StartMultiStageExchange"
      ],
      "properties": {
        "StartMultiStageExchange": {
          "type": "object",
          "required": [
            "mode"
          ],
          "properties": {
            "mode": {
              "$ref": "#/definitions/ExchangeMode"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "App layer should navigate to the link-mode initiator screen (`AppScreen::LinkExchange`).\n\nEmitted by `ExchangeEngine` when the user picks `ExchangeMode::Link` from the mode list, and by `LinkExchangeEngine` itself on the failed screen's Retry action. AppEngine routing navigates to the dedicated screen, whose factory builds a fresh `LinkExchangeEngine` and the AppEngine link-initiator lifecycle constructs the engine-owned `LinkInitiatorSession`. Frontends never see this — the relay escrow lifecycle is core/event-driven (no platform bridge).",
      "type": "string",
      "enum": [
        "StartLinkExchange"
      ]
    },
    {
      "description": "App layer should navigate to the dedicated BLE-exchange screen (`AppScreen::BleExchange { mode }`) for Magic/Bump/Shake. The screen factory builds a fresh `BleExchangeEngine`; the legacy `ExchangeStep::Ble` sub-flow is retired in slice 3. Per `2026-05-11-ble-exchange-engine-graduation`.",
      "type": "object",
      "required": [
        "StartBleExchange"
      ],
      "properties": {
        "StartBleExchange": {
          "type": "object",
          "required": [
            "mode"
          ],
          "properties": {
            "mode": {
              "$ref": "#/definitions/ExchangeMode"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "App layer should navigate to the dedicated NFC-exchange screen (`AppScreen::NfcExchange`) for TapTap. The screen factory builds a fresh `NfcExchangeEngine` (reconstructing the signing `Identity` from storage). Emitted by `ExchangeEngine` when the user picks `ExchangeMode::TapTap`, and by `NfcExchangeEngine` itself on the failed screen's Retry action (a fresh engine re-provisions the consumed, un-cloneable `Identity`). The legacy `ExchangeStep::Nfc` / `NfcRoleSelection` sub-flow is retired in a follow-up slice. Per `2026-05-11`-era exchange-engine graduation program.",
      "type": "string",
      "enum": [
        "StartNfcExchange"
      ]
    },
    {
      "description": "App layer should navigate to the dedicated DirectTransport (Cable/USB) exchange screen (`AppScreen::DirectTransport`). The screen factory builds a fresh `DirectTransportEngine` (re-provisioning the consumed `Identity`). Emitted by `ExchangeEngine` when the user picks `ExchangeMode::Cable`, and by `DirectTransportEngine` itself on the failed screen's Retry action. The legacy `ExchangeStep::DirectTransport` sub-flow is retired in slice 3. Per `2026-05-11-direct-transport-engine-graduation`.",
      "type": "string",
      "enum": [
        "StartDirectTransport"
      ]
    },
    {
      "description": "App layer should call `MobileDeviceLinkSession::confirm_manual` with the given confirmation code and the current unix timestamp.\n\nEmitted by `DeviceLinkingEngine` from the `ConfirmingDevice` step when the user taps \"codes match\" — the single confirmation since M5 B2b collapsed the redundant proximity screen. Pair 5 of `2026-04-28-pure-humble-ui-retire-native-screens`. Ultrasonic confirmation flows through ADR-031 hardware events instead.",
      "type": "object",
      "required": [
        "DeviceLinkConfirmManual"
      ],
      "properties": {
        "DeviceLinkConfirmManual": {
          "type": "object",
          "required": [
            "code"
          ],
          "properties": {
            "code": {
              "type": "string"
            }
          }
        }
      },
      "additionalProperties": false
    },
    {
      "description": "App layer should call `MobileDeviceLinkSession::deny`. Emitted by `DeviceLinkingEngine` from the `ConfirmingDevice` step when the user denies the request. The session's cycle thread fires `on_failed(\"user_denied\")` followed by `on_session_ended()`.",
      "type": "string",
      "enum": [
        "DeviceLinkDeny"
      ]
    },
    {
      "description": "App layer should create a fresh `MobileDeviceLinkSession` (single-shot — sessions cannot be reused) and call `start()`. Emitted by `DeviceLinkingEngine` from `QrExpired` and `LinkFailed` when the user taps Retry. The engine is already in the `QrPending` state when this is emitted; the new session will fire `on_qr_ready` to advance it.",
      "type": "string",
      "enum": [
        "DeviceLinkRetry"
      ]
    },
    {
      "description": "Outcome of the ADR-031 biometric-unlock hardware event ([`vauchi_core::Event::BiometricUnlockSucceeded`]). `Unlocked` proceeds straight to the post-auth screen; `PromptForDuressPin` tells the frontend to present the PIN entry screen so the subsequent `authenticate()` call can resolve Normal vs Duress. Replaces the legacy `PlatformAppEngine::biometric_unlock_check` typed getter (Track B of `2026-05-11-pure-functional-core-program`).",
      "type": "object",
      "required": [
        "BiometricUnlockOutcome"
      ],
      "properties": {
        "BiometricUnlockOutcome": {
          "type": "object",
          "required": [
            "outcome"
          ],
          "properties": {
            "outcome": {
              "$ref": "#/definitions/BiometricUnlockOutcome"
            }
          }
        }
      },
      "additionalProperties": false
    }
  ],
  "definitions": {
    "A11y": {
      "description": "Accessibility metadata for a UI component.\n\nPopulated by core engines so frontends can apply consistent a11y attributes without inventing their own labels.",
      "type": "object",
      "properties": {
        "hint": {
          "description": "Usage hint (maps to accessibilityHint/stateDescription).",
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "description": "Screen-reader label (maps to contentDescription/accessibilityLabel).",
          "type": [
            "string",
            "null"
          ]
        },
        "role": {
          "description": "Semantic role for screen readers.",
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/AccessibilityRole"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "AccessibilityRole": {
      "description": "Semantic role hint for screen readers.",
      "type": "string",
      "enum": [
        "Button",
        "Link",
        "Heading",
        "Image",
        "Toggle",
        "TextField",
        "Alert"
      ]
    },
    "AccessibilitySpec": {
      "type": "object",
      "required": [
        "label"
      ],
      "properties": {
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "type": "string"
        }
      }
    },
    "ActionListItem": {
      "description": "An item in an action list.",
      "type": "object",
      "required": [
        "id",
        "label"
      ],
      "properties": {
        "a11y": {
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/A11y"
            },
            {
              "type": "null"
            }
          ]
        },
        "detail": {
          "type": [
            "string",
            "null"
          ]
        },
        "icon": {
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "type": "string"
        },
        "info_key": {
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "type": "string"
        }
      }
    },
    "ActionSpec": {
      "type": "object",
      "required": [
        "accessibility_label",
        "enabled",
        "interaction_id",
        "label"
      ],
      "properties": {
        "accessibility_label": {
          "type": "string"
        },
        "enabled": {
          "type": "boolean"
        },
        "icon_token": {
          "type": [
            "string",
            "null"
          ]
        },
        "interaction_id": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "shortcut": {
          "anyOf": [
            {
              "$ref": "#/definitions/StandardShortcut"
            },
            {
              "type": "null"
            }
          ]
        },
        "tone": {
          "$ref": "#/definitions/ActionTone"
        }
      }
    },
    "ActionTone": {
      "type": "string",
      "enum": [
        "standard",
        "destructive"
      ]
    },
    "AlertSpec": {
      "type": "object",
      "required": [
        "message",
        "title"
      ],
      "properties": {
        "message": {
          "type": "string"
        },
        "title": {
          "type": "string"
        }
      }
    },
    "AnimationToken": {
      "description": "Animation intent for [`Command::Celebrate`]. Closed enum — platforms map to a native one-beat animation, respecting reduce-motion.",
      "oneOf": [
        {
          "description": "The two cards meet — the exchange-success beat (~600 ms, then still).",
          "type": "string",
          "enum": [
            "cards_meet"
          ]
        },
        {
          "description": "No animation.",
          "type": "string",
          "enum": [
            "none"
          ]
        }
      ]
    },
    "AuthenticationRequirement": {
      "description": "Authentication state the native startup shell must present.",
      "oneOf": [
        {
          "description": "Biometric authentication completed and normal app state may be shown.",
          "type": "string",
          "enum": [
            "unlocked"
          ]
        },
        {
          "description": "A configured duress PIN requires the native app-password prompt.",
          "type": "string",
          "enum": [
            "app_password"
          ]
        }
      ]
    },
    "BiometricUnlockOutcome": {
      "description": "Outcome of [`crate::api::Vauchi::biometric_unlock_check`].\n\nReturned after a successful platform biometric authentication (LAContext on iOS, BiometricPrompt on Android). The variant tells the frontend which screen to render next:\n\n- `Unlocked`: biometric proves the real user; transition to the post-auth screen. - `PromptForDuressPin`: a duress PIN is configured, so the user must enter the PIN — that PIN check determines `Normal` vs `Duress` mode via [`crate::api::Vauchi::authenticate`].\n\nThe dispatcher is constant-time: the wall-clock duration of the containing call is at least [`crate::api::BIOMETRIC_UNLOCK_MIN_DURATION`] regardless of which outcome is returned, so an observer cannot infer whether duress is configured by timing the unlock animation.",
      "oneOf": [
        {
          "description": "Biometric authentication succeeded and no duress PIN is configured — the user is fully unlocked. `auth_mode` is set to [`crate::api::AuthMode::Normal`].",
          "type": "string",
          "enum": [
            "Unlocked"
          ]
        },
        {
          "description": "Biometric authentication succeeded but a duress PIN is configured — the frontend must present the PIN entry screen so the user enters either the real PIN or the duress PIN. The subsequent [`crate::api::Vauchi::authenticate`] call sets the final [`crate::api::AuthMode`].",
          "type": "string",
          "enum": [
            "PromptForDuressPin"
          ]
        }
      ]
    },
    "BleLinkDirection": {
      "description": "Which physical direction a BLE link was established in, reported by the shell on [`Event::BleConnected`]. This is the ground truth for the exchange protocol role: the GATT **central** (it dialed out) drives the handshake as initiator; the GATT **peripheral** (it was connected to) responds. Deriving the role from the physical link — not the pre-connection token tiebreak — is what lets asymmetric BLE discovery self-heal: whichever side actually connects becomes the initiator, even if the token said otherwise (`_private/docs/designs/2026-07-22-role-tiebreak-and-glare-design.md`).",
      "oneOf": [
        {
          "description": "This device is the GATT central — it dialed out to the peer's peripheral.",
          "type": "string",
          "enum": [
            "Outbound"
          ]
        },
        {
          "description": "This device is the GATT peripheral — the peer connected to it.",
          "type": "string",
          "enum": [
            "Inbound"
          ]
        }
      ]
    },
    "BorderRadiusTokens": {
      "description": "Border radius tokens for rounded corners.",
      "type": "object",
      "required": [
        "lg",
        "md",
        "md_lg",
        "sm"
      ],
      "properties": {
        "card": {
          "default": 20,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "chip": {
          "default": 12,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "lg": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "md": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "md_lg": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "sheet": {
          "default": 28,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "sm": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        }
      }
    },
    "ChoiceOption": {
      "type": "object",
      "required": [
        "id",
        "label"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "label": {
          "type": "string"
        }
      }
    },
    "Command": {
      "description": "A command from core to the frontend requesting a hardware action.\n\nFrontends match on these and dispatch to platform-specific APIs (camera, BLE stack, NFC reader, audio subsystem). Commands that the platform cannot fulfil should be answered with [`Event::HardwareUnavailable`].",
      "oneOf": [
        {
          "description": "Display a QR code containing `data`.",
          "type": "object",
          "required": [
            "QrDisplay"
          ],
          "properties": {
            "QrDisplay": {
              "type": "object",
              "required": [
                "data"
              ],
              "properties": {
                "data": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Request the frontend to open a QR scanner (camera).",
          "type": "string",
          "enum": [
            "QrRequestScan"
          ]
        },
        {
          "description": "Core-computed wakeup schedule (ADR-030/031 command + ADR-044 Am2a: generic timer *data*, not a per-feature flag). Core stays the single authority on *when* the app-level heartbeat is due; the humble shell translates this into its platform wakeup — a desktop in-process interval/thread, iOS `BGAppRefreshTask`, Android `WorkManager` — and, when it fires, calls `AppEngine::on_wakeup()`, which runs due work and emits the *next* `ScheduleWakeup`. All fields are relative seconds from now (the OS APIs take intervals, and it frees the shell from reading core's clock). Every interval is a *hint*: the OS may fire late, coalesce, or skip — `on_wakeup` is elapsed-based and idempotent, so a missed or doubled wake is safe.",
          "type": "object",
          "required": [
            "ScheduleWakeup"
          ],
          "properties": {
            "ScheduleWakeup": {
              "type": "object",
              "required": [
                "deadline_secs",
                "earliest_secs",
                "min_interval_secs"
              ],
              "properties": {
                "deadline_secs": {
                  "description": "Latest the work should ideally run by; the shell picks a higher-fidelity mechanism if this is tight.",
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0.0
                },
                "earliest_secs": {
                  "description": "Earliest the shell should wake core (never earlier — battery floor).",
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0.0
                },
                "min_interval_secs": {
                  "description": "Minimum spacing between wakes the shell should honor (coalesce).",
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0.0
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Start advertising the vauchi BLE service with the given payload.",
          "type": "object",
          "required": [
            "BleStartAdvertising"
          ],
          "properties": {
            "BleStartAdvertising": {
              "type": "object",
              "required": [
                "payload",
                "service_uuid"
              ],
              "properties": {
                "payload": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                },
                "service_uuid": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Start scanning for vauchi BLE peripherals.",
          "type": "object",
          "required": [
            "BleStartScanning"
          ],
          "properties": {
            "BleStartScanning": {
              "type": "object",
              "required": [
                "service_uuid"
              ],
              "properties": {
                "service_uuid": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Connect to a discovered BLE device.",
          "type": "object",
          "required": [
            "BleConnect"
          ],
          "properties": {
            "BleConnect": {
              "type": "object",
              "required": [
                "device_id"
              ],
              "properties": {
                "device_id": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Write data to a BLE characteristic on the link to `device_id`.\n\n`device_id` addresses the target link so shells holding more than one live link (glare) route the write to the right peer instead of an implicit \"current\" connection (`_private/docs/designs/2026-07-22-role-tiebreak-and-glare-design.md`).",
          "type": "object",
          "required": [
            "BleWriteCharacteristic"
          ],
          "properties": {
            "BleWriteCharacteristic": {
              "type": "object",
              "required": [
                "data",
                "device_id",
                "direction",
                "uuid"
              ],
              "properties": {
                "data": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                },
                "device_id": {
                  "type": "string"
                },
                "direction": {
                  "$ref": "#/definitions/BleLinkDirection"
                },
                "uuid": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Read data from a BLE characteristic on the link to `device_id`.",
          "type": "object",
          "required": [
            "BleReadCharacteristic"
          ],
          "properties": {
            "BleReadCharacteristic": {
              "type": "object",
              "required": [
                "device_id",
                "direction",
                "uuid"
              ],
              "properties": {
                "device_id": {
                  "type": "string"
                },
                "direction": {
                  "$ref": "#/definitions/BleLinkDirection"
                },
                "uuid": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Disconnect one specific BLE link, addressed by peer and direction.\n\nTargeted (not \"disconnect everything\") so glare resolution can drop exactly the losing link while the surviving link keeps carrying the handshake.",
          "type": "object",
          "required": [
            "BleDisconnect"
          ],
          "properties": {
            "BleDisconnect": {
              "type": "object",
              "required": [
                "device_id",
                "direction"
              ],
              "properties": {
                "device_id": {
                  "type": "string"
                },
                "direction": {
                  "$ref": "#/definitions/BleLinkDirection"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Activate the NFC interface and prepare to exchange `payload`.",
          "type": "object",
          "required": [
            "NfcActivate"
          ],
          "properties": {
            "NfcActivate": {
              "type": "object",
              "required": [
                "payload"
              ],
              "properties": {
                "payload": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Deactivate the NFC interface.",
          "type": "string",
          "enum": [
            "NfcDeactivate"
          ]
        },
        {
          "description": "Send a continuation APDU on the already-active NFC session. Used by the multi-phase handshake after the initial `NfcActivate`: the frontend keeps the tag connection alive and transmits `data` opaquely, then surfaces the peer's response as `Event::NfcDataReceived`.",
          "type": "object",
          "required": [
            "NfcSendApdu"
          ],
          "properties": {
            "NfcSendApdu": {
              "type": "object",
              "required": [
                "data"
              ],
              "properties": {
                "data": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Emit ultrasonic PCM samples encoding a challenge.\n\nCore has already FSK-encoded the challenge bytes; the frontend just plays the samples through the device speaker. Mono float PCM at `sample_rate`.",
          "type": "object",
          "required": [
            "AudioEmitChallenge"
          ],
          "properties": {
            "AudioEmitChallenge": {
              "type": "object",
              "required": [
                "sample_rate",
                "samples"
              ],
              "properties": {
                "sample_rate": {
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0.0
                },
                "samples": {
                  "type": "array",
                  "items": {
                    "type": "number",
                    "format": "float"
                  }
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Listen for an ultrasonic response within `timeout_ms`.\n\n`sample_rate` is the suggested capture rate; if the device's preferred rate differs, the frontend reports its actual rate in [`Event::AudioSamplesRecorded`].",
          "type": "object",
          "required": [
            "AudioListenForResponse"
          ],
          "properties": {
            "AudioListenForResponse": {
              "type": "object",
              "required": [
                "sample_rate",
                "timeout_ms"
              ],
              "properties": {
                "sample_rate": {
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0.0
                },
                "timeout_ms": {
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0.0
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Stop all audio operations.",
          "type": "string",
          "enum": [
            "AudioStop"
          ]
        },
        {
          "description": "Start accelerometer sampling for proximity verification.",
          "type": "string",
          "enum": [
            "AccelerometerStart"
          ]
        },
        {
          "description": "Stop accelerometer sampling.",
          "type": "string",
          "enum": [
            "AccelerometerStop"
          ]
        },
        {
          "description": "Deposit encrypted card into relay escrow gate.",
          "type": "object",
          "required": [
            "RelayEscrowDeposit"
          ],
          "properties": {
            "RelayEscrowDeposit": {
              "type": "object",
              "required": [
                "encrypted_card",
                "gate_hash",
                "slot_hash",
                "ttl_seconds"
              ],
              "properties": {
                "encrypted_card": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                },
                "gate_hash": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                },
                "slot_hash": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                },
                "ttl_seconds": {
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0.0
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Check relay escrow gate readiness (poll until ready).\n\nFrontends should poll at `suggested_interval_ms` with exponential backoff (cap at 5 min). Report `RelayEscrowReady` when gate has ≥2 deposits, or `RelayEscrowFailed` on error/timeout.",
          "type": "object",
          "required": [
            "RelayEscrowCheck"
          ],
          "properties": {
            "RelayEscrowCheck": {
              "type": "object",
              "required": [
                "gate_hash",
                "suggested_interval_ms"
              ],
              "properties": {
                "gate_hash": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                },
                "suggested_interval_ms": {
                  "description": "Suggested initial polling interval in milliseconds.",
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0.0
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Retrieve blob from relay escrow gate.",
          "type": "object",
          "required": [
            "RelayEscrowRetrieve"
          ],
          "properties": {
            "RelayEscrowRetrieve": {
              "type": "object",
              "required": [
                "gate_hash",
                "slot_hash"
              ],
              "properties": {
                "gate_hash": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                },
                "slot_hash": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Show system share sheet with a URL.",
          "type": "object",
          "required": [
            "ShowShareSheet"
          ],
          "properties": {
            "ShowShareSheet": {
              "type": "object",
              "required": [
                "url"
              ],
              "properties": {
                "url": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Stop BLE scanning (saves battery after discovery completes).",
          "type": "string",
          "enum": [
            "BleStopScanning"
          ]
        },
        {
          "description": "Send an exchange payload over a direct transport (USB cable / local TCP).\n\nThe frontend should: 1. Send `payload` to the peer over the established TCP connection 2. Receive the peer's payload from the same connection 3. Report the peer's data via [`Event::DirectPayloadReceived`]\n\nThe `is_initiator` flag determines send/recv ordering to avoid deadlock (initiator sends first, responder receives first).",
          "type": "object",
          "required": [
            "DirectSend"
          ],
          "properties": {
            "DirectSend": {
              "type": "object",
              "required": [
                "is_initiator",
                "payload"
              ],
              "properties": {
                "is_initiator": {
                  "type": "boolean"
                },
                "payload": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Request the frontend to open the device photo library / gallery.\n\nFrontend should return [`Event::ImageReceived`] with the selected image bytes, or [`Event::ImagePickCancelled`] if the user dismisses the picker.",
          "type": "string",
          "enum": [
            "ImagePickFromLibrary"
          ]
        },
        {
          "description": "Request the frontend to capture an image from the device camera.\n\nFrontend should return [`Event::ImageReceived`] with the captured image bytes, or [`Event::ImagePickCancelled`] if the user cancels.",
          "type": "string",
          "enum": [
            "ImageCaptureFromCamera"
          ]
        },
        {
          "description": "Request the frontend to open a file picker for image files.\n\nUsed on desktop platforms where a photo library may not exist. Frontend should return [`Event::ImageReceived`] with the selected image bytes, or [`Event::ImagePickCancelled`] if the user cancels.",
          "type": "string",
          "enum": [
            "ImagePickFromFile"
          ]
        },
        {
          "description": "Switch the active camera between front- and rear-facing.\n\nUsed by the multi-stage face-to-face exchange screen so the user can flip the scanner orientation without the frontend owning the preference. `use_front == true` selects the front camera.",
          "type": "object",
          "required": [
            "SwitchCamera"
          ],
          "properties": {
            "SwitchCamera": {
              "type": "object",
              "required": [
                "use_front"
              ],
              "properties": {
                "use_front": {
                  "type": "boolean"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Request the frontend to open a file picker.\n\n`accepted_mime_types` is advisory — frontends may default to a coarser superset on platforms where the OS picker doesn't filter by MIME (e.g., older Android versions). `purpose` lets the frontend label the picker dialog without hardcoding strings; label text comes from core's locale store via `t(key)`.\n\nFrontend should return [`Event::FilePickedFromUser`] with the selected file's bytes, or [`Event::FilePickCancelledByUser`] if the user dismisses the picker.\n\nDistinct from [`Command::ImagePickFromFile`]: that variant returns [`Event::ImageReceived`] which is shaped for avatar normalization. File picking returns raw bytes plus filename for arbitrary payloads (vCard, encrypted backup blob, future key bundles, etc.).",
          "type": "object",
          "required": [
            "FilePickFromUser"
          ],
          "properties": {
            "FilePickFromUser": {
              "type": "object",
              "required": [
                "accepted_mime_types",
                "purpose"
              ],
              "properties": {
                "accepted_mime_types": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "purpose": {
                  "$ref": "#/definitions/FilePickPurpose"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Set the device screen brightness, optionally restoring the platform default when `level` is `None`.\n\nUsed by screens that need a specific brightness for their hardware to function (e.g., the multi-stage face-to-face exchange uses 65% brightness so the front camera is not over-exposed when scanning a peer's QR). The frontend is responsible for snapshotting the prior value on the *first* `Some(level)` after a `None` (or app start) so the subsequent `None` correctly restores it.\n\nFrontends that have no programmatic brightness control (e.g., desktop, where the OS owns it) answer with [`Event::HardwareUnavailable { transport: \"screen_brightness\" }`] — core should treat that as \"request honoured at platform default\" and not retry.\n\nPer `2026-05-01-screen-id-metadata-in-core` cousin `2026-05-04-exchange-command-screen-presentation`, ADR-031 §Hardware. Phase 1 of the FaceToFaceExchangeView retirement.",
          "type": "object",
          "required": [
            "SetScreenBrightness"
          ],
          "properties": {
            "SetScreenBrightness": {
              "type": "object",
              "properties": {
                "level": {
                  "type": [
                    "number",
                    "null"
                  ],
                  "format": "float"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Disable or re-enable the platform's idle / auto-lock timer.\n\n`disabled = true` keeps the screen awake (used by the multi-stage exchange so a longer-than-30s handshake doesn't trigger the device's auto-lock). `disabled = false` restores the platform default on screen exit. Idempotent — frontends MAY ignore a redundant set/clear.\n\nFrontends that have no programmatic idle-timer control answer with [`Event::HardwareUnavailable { transport: \"idle_timer\" }`].\n\nPhase 1 of the FaceToFaceExchangeView retirement (companion to [`Command::SetScreenBrightness`]).",
          "type": "object",
          "required": [
            "SetIdleTimerDisabled"
          ],
          "properties": {
            "SetIdleTimerDisabled": {
              "type": "object",
              "required": [
                "disabled"
              ],
              "properties": {
                "disabled": {
                  "type": "boolean"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Lock or unlock the device's screen orientation.\n\n`Some(orientation)` pins the screen to the requested orientation (e.g., `Portrait` so the multi-stage face-to-face exchange QR / camera layout stays stable while the user is moving the device); `None` restores the platform default (typically follows the device's physical rotation).\n\nFrontends that don't programmatically own orientation (`linux-gtk`, `linux-qt`, `windows`, desktop `macos` — windowed apps don't lock device rotation) answer with [`Event::HardwareUnavailable { transport: \"orientation_lock\" }`].\n\nPhase 2c of `2026-05-04-exchange-command-screen-presentation` — retires the orientation `DisposableEffect` in `android/app/src/main/kotlin/app/vauchi/ui/FaceToFaceExchangeScreen.kt`.",
          "type": "object",
          "required": [
            "SetOrientationLock"
          ],
          "properties": {
            "SetOrientationLock": {
              "type": "object",
              "properties": {
                "orientation": {
                  "anyOf": [
                    {
                      "$ref": "#/definitions/Orientation"
                    },
                    {
                      "type": "null"
                    }
                  ]
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Send our AEAD-encrypted `ContactCard` to the peer over the established USB / direct-TCP connection (the second leg of the wired exchange, after the key-bearing [`Command::DirectSend`] / [`Event::DirectPayloadReceived`] round). `ciphertext` is our card encrypted under a key HKDF-derived from the agreed `shared_key` (ADR-019 XChaCha20-Poly1305, ADR-007 domain separation). The frontend sends it and reports the peer's encrypted card via [`Event::DirectCardReceived`]. `is_initiator` keeps the same send/recv ordering discriminator as `DirectSend` (avoids TCP deadlock).",
          "type": "object",
          "required": [
            "DirectSendCard"
          ],
          "properties": {
            "DirectSendCard": {
              "type": "object",
              "required": [
                "ciphertext",
                "is_initiator"
              ],
              "properties": {
                "ciphertext": {
                  "type": "array",
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                },
                "is_initiator": {
                  "type": "boolean"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Request the device's current location (ADR-051 contact annotations): used at an in-person exchange to record *where we met*. The frontend resolves permission and replies with [`Event::LocationResult`], or [`Event::PermissionDenied { transport: \"location\" }`] if the user declines, or [`Event::HardwareUnavailable`] if there is no location provider. `timeout_ms` bounds how long the frontend waits for a fix.",
          "type": "object",
          "required": [
            "LocationRequest"
          ],
          "properties": {
            "LocationRequest": {
              "type": "object",
              "required": [
                "timeout_ms"
              ],
              "properties": {
                "timeout_ms": {
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0.0
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Render the exchange-success ceremony: a celebratory haptic + sound + animation (`designs/2026-06-06-exchange-ceremony-design.md`). The tokens are closed intent enums looked up against a static per-platform table (Wire-Humble kind rule) — core never sends assets or frequencies. Frontends skip any axis they can't render; the others still play. Emitted exactly once per validated exchange success, never on failure, and byte-identical regardless of auth mode (ADR-032 duress parity).",
          "type": "object",
          "required": [
            "Celebrate"
          ],
          "properties": {
            "Celebrate": {
              "type": "object",
              "required": [
                "animation",
                "haptic",
                "sound"
              ],
              "properties": {
                "animation": {
                  "$ref": "#/definitions/AnimationToken"
                },
                "haptic": {
                  "$ref": "#/definitions/HapticPattern"
                },
                "sound": {
                  "$ref": "#/definitions/SoundToken"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Atomically replace the contextual controls associated with a surface.",
          "type": "object",
          "required": [
            "SetContextBar"
          ],
          "properties": {
            "SetContextBar": {
              "type": "object",
              "required": [
                "bar",
                "revision",
                "surface_id"
              ],
              "properties": {
                "bar": {
                  "$ref": "#/definitions/ContextBar"
                },
                "revision": {
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0.0
                },
                "surface_id": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Present navigation or secondary actions for a surface.",
          "type": "object",
          "required": [
            "PresentOverlay"
          ],
          "properties": {
            "PresentOverlay": {
              "type": "object",
              "required": [
                "overlay",
                "revision",
                "surface_id"
              ],
              "properties": {
                "overlay": {
                  "$ref": "#/definitions/OverlaySpec"
                },
                "revision": {
                  "type": "integer",
                  "format": "uint64",
                  "minimum": 0.0
                },
                "surface_id": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Apply Core's structural composition for the current window.",
          "type": "object",
          "required": [
            "SetPresentationProfile"
          ],
          "properties": {
            "SetPresentationProfile": {
              "type": "object",
              "required": [
                "profile"
              ],
              "properties": {
                "profile": {
                  "$ref": "#/definitions/PresentationProfile"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Atomically replace one named surface with fully prepared, domain-free presentation primitives.",
          "type": "object",
          "required": [
            "ReplaceSurface"
          ],
          "properties": {
            "ReplaceSurface": {
              "type": "object",
              "required": [
                "surface"
              ],
              "properties": {
                "surface": {
                  "$ref": "#/definitions/SurfaceSpec"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Present a prepared native alert.",
          "type": "object",
          "required": [
            "PresentAlert"
          ],
          "properties": {
            "PresentAlert": {
              "type": "object",
              "required": [
                "alert"
              ],
              "properties": {
                "alert": {
                  "$ref": "#/definitions/AlertSpec"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Present transient informational feedback.",
          "type": "object",
          "required": [
            "ShowToast"
          ],
          "properties": {
            "ShowToast": {
              "type": "object",
              "required": [
                "toast"
              ],
              "properties": {
                "toast": {
                  "$ref": "#/definitions/ToastSpec"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Open a prepared URL using the platform default handler.",
          "type": "object",
          "required": [
            "OpenExternalUrl"
          ],
          "properties": {
            "OpenExternalUrl": {
              "type": "object",
              "required": [
                "url"
              ],
              "properties": {
                "url": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Offer prepared bytes through the platform save/share mechanism.",
          "type": "object",
          "required": [
            "ExportFile"
          ],
          "properties": {
            "ExportFile": {
              "type": "object",
              "required": [
                "file"
              ],
              "properties": {
                "file": {
                  "$ref": "#/definitions/ExportFileSpec"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Perform the platform's default back behavior.",
          "type": "string",
          "enum": [
            "PerformNativeBack"
          ]
        },
        {
          "description": "Reset platform-owned application state after Core wiped its data.",
          "type": "string",
          "enum": [
            "ResetApplication"
          ]
        },
        {
          "description": "Post a fully prepared operating-system notification.",
          "type": "object",
          "required": [
            "PostNotification"
          ],
          "properties": {
            "PostNotification": {
              "type": "object",
              "required": [
                "notification"
              ],
              "properties": {
                "notification": {
                  "$ref": "#/definitions/NotificationSpec"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Apply Core's post-biometric authentication decision.",
          "type": "object",
          "required": [
            "SetAuthenticationRequirement"
          ],
          "properties": {
            "SetAuthenticationRequirement": {
              "type": "object",
              "required": [
                "requirement"
              ],
              "properties": {
                "requirement": {
                  "$ref": "#/definitions/AuthenticationRequirement"
                }
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "Component": {
      "description": "A UI component that core tells frontends to render.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "Divider"
          ]
        },
        {
          "type": "object",
          "required": [
            "Text"
          ],
          "properties": {
            "Text": {
              "type": "object",
              "required": [
                "content",
                "id",
                "style"
              ],
              "properties": {
                "content": {
                  "type": "string"
                },
                "id": {
                  "type": "string"
                },
                "style": {
                  "$ref": "#/definitions/TextStyle"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "TextInput"
          ],
          "properties": {
            "TextInput": {
              "type": "object",
              "required": [
                "id",
                "input_type",
                "label",
                "value"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "id": {
                  "type": "string"
                },
                "info_key": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "input_type": {
                  "$ref": "#/definitions/InputType"
                },
                "label": {
                  "type": "string"
                },
                "max_length": {
                  "type": [
                    "integer",
                    "null"
                  ],
                  "format": "uint",
                  "minimum": 0.0
                },
                "placeholder": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "validation_error": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "value": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "ToggleList"
          ],
          "properties": {
            "ToggleList": {
              "type": "object",
              "required": [
                "id",
                "items",
                "label"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "id": {
                  "type": "string"
                },
                "items": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/ToggleItem"
                  }
                },
                "label": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "FieldList"
          ],
          "properties": {
            "FieldList": {
              "type": "object",
              "required": [
                "available_scopes",
                "fields",
                "id",
                "visibility_mode"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "available_scopes": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "fields": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/Field"
                  }
                },
                "id": {
                  "type": "string"
                },
                "title": {
                  "description": "Core-resolved section label. Renderers use this verbatim for native group accessibility; terminal layouts may also show it as the panel title. An empty value only occurs on legacy payloads.",
                  "default": "",
                  "type": "string"
                },
                "visibility_mode": {
                  "$ref": "#/definitions/VisibilityMode"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Preview"
          ],
          "properties": {
            "Preview": {
              "type": "object",
              "required": [
                "fields",
                "initials",
                "name",
                "variants"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "fields": {
                  "description": "All fields on the card (raw — retained for backwards compatibility).\n\nFrontends should render `visible_fields` instead. This field is kept additive for one binding cycle so consumers can migrate without an ABI break; a follow-up MR removes it.",
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/Field"
                  }
                },
                "image_data": {
                  "description": "Image bytes (WebP). Frontends show this in the circular header area. Falls back to `initials` when None.",
                  "type": [
                    "array",
                    "null"
                  ],
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                },
                "initials": {
                  "description": "Core-derived avatar initials (first letters of the first two words of `name`, uppercased). Frontends render this directly in the initials fallback — never recompute `name.take(1)` (ADR-021/043 Humble UI).",
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "selected_variant": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "variants": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/PreviewVariant"
                  }
                },
                "visible_fields": {
                  "description": "Pre-filtered fields to render — what the user actually sees.\n\nComputed by [`build_visible_fields`]: when `selected_variant` is set and matches a `PreviewVariant`, returns that group's `visible_fields`. Otherwise returns `fields` filtered to keep only `Shown` and `Groups` visibility variants. Frontends render this list directly — no `.filter` over `fields` should appear in view code (ADR-021/043 Humble UI).",
                  "default": [],
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/Field"
                  }
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "InfoPanel"
          ],
          "properties": {
            "InfoPanel": {
              "type": "object",
              "required": [
                "id",
                "items",
                "title"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "icon": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "id": {
                  "type": "string"
                },
                "items": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/InfoItem"
                  }
                },
                "title": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A list of items rendered in a uniform row layout, with optional search affordance. Frontends bind on `items`; the renderer doesn't know what kind of thing is in the list (per ADR-021/043 + Wire Humble). Engines that want to render lists of any domain emit this variant with their data mapped to `Vec<Item>`.",
          "type": "object",
          "required": [
            "List"
          ],
          "properties": {
            "List": {
              "type": "object",
              "required": [
                "id",
                "items",
                "searchable"
              ],
              "properties": {
                "id": {
                  "type": "string"
                },
                "items": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/Item"
                  }
                },
                "offset": {
                  "description": "Window start within the filtered set (windowed emissions only).",
                  "type": "integer",
                  "format": "uint",
                  "minimum": 0.0
                },
                "searchable": {
                  "type": "boolean"
                },
                "total_count": {
                  "description": "Size of the full filtered set when the emission is windowed; zero/absent = unwindowed (`items` is the complete set, the exact pre-windowing wire shape). Windowing keeps multi-MB emissions off the wire at 10k items (`2026-06-11-contacts-list-eager-render-anr` Track B).",
                  "type": "integer",
                  "format": "uint",
                  "minimum": 0.0
                },
                "window": {
                  "description": "Emitted window length. The renderer dispatches [`crate::ui::UserAction::ListWindowRequested`] as scrolling approaches the loaded window's edge.",
                  "type": "integer",
                  "format": "uint",
                  "minimum": 0.0
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "SettingsGroup"
          ],
          "properties": {
            "SettingsGroup": {
              "type": "object",
              "required": [
                "id",
                "items",
                "label"
              ],
              "properties": {
                "id": {
                  "type": "string"
                },
                "items": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/SettingsItem"
                  }
                },
                "label": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "ActionList"
          ],
          "properties": {
            "ActionList": {
              "type": "object",
              "required": [
                "id",
                "items"
              ],
              "properties": {
                "id": {
                  "type": "string"
                },
                "items": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/ActionListItem"
                  }
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A horizontal container — lays its child components out in a single row. Used to place a camera preview beside its action buttons so a fixed-layout screen fits the viewport without scrolling (`2026-06-03-exchange-qr-scan-stability`). The first child flexes; later children take their natural width.",
          "type": "object",
          "required": [
            "Row"
          ],
          "properties": {
            "Row": {
              "type": "object",
              "required": [
                "id",
                "items"
              ],
              "properties": {
                "id": {
                  "type": "string"
                },
                "items": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/Component"
                  }
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "StatusIndicator"
          ],
          "properties": {
            "StatusIndicator": {
              "type": "object",
              "required": [
                "id",
                "status",
                "title"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "detail": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "icon": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "id": {
                  "type": "string"
                },
                "status": {
                  "$ref": "#/definitions/Status"
                },
                "status_label": {
                  "description": "Core-resolved localized badge label for `status`. Frontends render it verbatim — deriving text from the discriminant is the W-class leak this field retires (`2026-07-06-mobile-domain-shell-violations`). Serde default keeps pre-field payloads deserializable.",
                  "default": "",
                  "type": "string"
                },
                "title": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "PinInput"
          ],
          "properties": {
            "PinInput": {
              "type": "object",
              "required": [
                "filled",
                "id",
                "label",
                "length",
                "masked"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "filled": {
                  "type": "integer",
                  "format": "uint",
                  "minimum": 0.0
                },
                "id": {
                  "type": "string"
                },
                "label": {
                  "type": "string"
                },
                "length": {
                  "type": "integer",
                  "format": "uint",
                  "minimum": 0.0
                },
                "masked": {
                  "type": "boolean"
                },
                "validation_error": {
                  "type": [
                    "string",
                    "null"
                  ]
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "QrCode"
          ],
          "properties": {
            "QrCode": {
              "type": "object",
              "required": [
                "data",
                "id",
                "mode"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "data": {
                  "type": "string"
                },
                "frames": {
                  "description": "Pre-rendered opaque animated-QR frames. Each entry is the same representation as `data` (a raw string the frontend encodes into one QR), so the renderer draws any frame exactly as it draws the static `data`. When non-empty the frontend animates through the frames while the component is mounted; when empty it renders the single static `data`. Core owns the chunking — the frames are opaque to the frontend (ADR-044 Amendment 2a: animated QR is render data, not a frontend behavior).",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "id": {
                  "type": "string"
                },
                "label": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "mode": {
                  "$ref": "#/definitions/QrMode"
                },
                "scan_quality": {
                  "description": "Real-time scan quality for the viewfinder frame indicator. Only meaningful when `mode` is `Scan`; `None` when `Display`.",
                  "anyOf": [
                    {
                      "$ref": "#/definitions/ScanQuality"
                    },
                    {
                      "type": "null"
                    }
                  ]
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Visible inline confirmation choices for irrevocable actions.",
          "type": "object",
          "required": [
            "InlineConfirm"
          ],
          "properties": {
            "InlineConfirm": {
              "type": "object",
              "required": [
                "cancel_action_id",
                "cancel_text",
                "confirm_action_id",
                "confirm_text",
                "destructive",
                "id",
                "warning"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "cancel_action_id": {
                  "type": "string"
                },
                "cancel_text": {
                  "type": "string"
                },
                "confirm_action_id": {
                  "description": "Opaque action ids the renderer forwards verbatim. A frontend must never derive these from `id` or button text.",
                  "type": "string"
                },
                "confirm_text": {
                  "description": "Core-owned button copy; render verbatim.",
                  "type": "string"
                },
                "destructive": {
                  "description": "If true, render confirm button in destructive/red style.",
                  "type": "boolean"
                },
                "id": {
                  "type": "string"
                },
                "warning": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A text field that toggles between display and edit mode.",
          "type": "object",
          "required": [
            "EditableText"
          ],
          "properties": {
            "EditableText": {
              "type": "object",
              "required": [
                "cancel_action_id",
                "cancel_text",
                "edit_action_id",
                "edit_text",
                "editing",
                "id",
                "label",
                "save_action_id",
                "save_text",
                "value"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "cancel_action_id": {
                  "type": "string"
                },
                "cancel_text": {
                  "type": "string"
                },
                "edit_action_id": {
                  "description": "Opaque transition action ids owned by the workflow engine.",
                  "type": "string"
                },
                "edit_text": {
                  "description": "Core-owned action copy; render verbatim.",
                  "type": "string"
                },
                "editing": {
                  "description": "When true, render as editable input. When false, render as static text with edit button.",
                  "type": "boolean"
                },
                "id": {
                  "type": "string"
                },
                "info_key": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "label": {
                  "type": "string"
                },
                "save_action_id": {
                  "type": "string"
                },
                "save_text": {
                  "type": "string"
                },
                "validation_error": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "value": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Informational banner with an optional action button (e.g. preview mode indicator).",
          "type": "object",
          "required": [
            "Banner"
          ],
          "properties": {
            "Banner": {
              "type": "object",
              "required": [
                "action_id",
                "action_label",
                "text"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "action_id": {
                  "type": "string"
                },
                "action_label": {
                  "type": "string"
                },
                "text": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "An inline dropdown for selection UIs (e.g. theme, language). Reuses `UserAction::ListItemSelected` — no new action variant needed.",
          "type": "object",
          "required": [
            "Dropdown"
          ],
          "properties": {
            "Dropdown": {
              "type": "object",
              "required": [
                "id",
                "label",
                "options"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "id": {
                  "type": "string"
                },
                "label": {
                  "type": "string"
                },
                "options": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/DropdownOption"
                  }
                },
                "selected": {
                  "type": [
                    "string",
                    "null"
                  ]
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Circular avatar preview with optional brightness adjustment.\n\nUsed in the Avatar Editor screen and as a display component. When `editable`, tapping forwards the core-supplied `edit_action_id` as `UserAction::ActionPressed` — the frontend never mints the id.",
          "type": "object",
          "required": [
            "ImageCircle"
          ],
          "properties": {
            "ImageCircle": {
              "type": "object",
              "required": [
                "id",
                "initials"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "bg_color": {
                  "description": "Background color for initials fallback `[r, g, b]`.",
                  "type": [
                    "array",
                    "null"
                  ],
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  },
                  "maxItems": 3,
                  "minItems": 3
                },
                "brightness": {
                  "description": "Brightness adjustment (-0.3 to 0.3). 0.0 = no change.",
                  "default": 0.0,
                  "type": "number",
                  "format": "float"
                },
                "edit_action_id": {
                  "description": "Action id the frontend forwards on tap when `editable`; core owns it (never minted frontend-side). `None` when not editable.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "editable": {
                  "description": "Whether tapping opens the avatar editor (own card only).",
                  "default": false,
                  "type": "boolean"
                },
                "id": {
                  "type": "string"
                },
                "image_data": {
                  "description": "Raw image bytes (WebP/PNG/JPEG) to display, or None for initials-only fallback.",
                  "type": [
                    "array",
                    "null"
                  ],
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                },
                "initials": {
                  "description": "Fallback initials text when no image is available.",
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A range slider for continuous value input.\n\nEmits `UserAction::SliderChanged { component_id, value }` on value changes.",
          "type": "object",
          "required": [
            "Slider"
          ],
          "properties": {
            "Slider": {
              "type": "object",
              "required": [
                "id",
                "label",
                "max",
                "min",
                "value"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "id": {
                  "type": "string"
                },
                "label": {
                  "type": "string"
                },
                "max": {
                  "description": "Maximum allowed value.",
                  "type": "number",
                  "format": "float"
                },
                "max_icon": {
                  "description": "Optional icon name for the max end (e.g., \"sun.max\").",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "min": {
                  "description": "Minimum allowed value.",
                  "type": "number",
                  "format": "float"
                },
                "min_icon": {
                  "description": "Optional icon name for the min end (e.g., \"sun.min\").",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "step": {
                  "description": "Step increment (0.0 = continuous).",
                  "default": 0.0,
                  "type": "number",
                  "format": "float"
                },
                "value": {
                  "description": "Current value.",
                  "type": "number",
                  "format": "float"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Ongoing-status indicator carrying a terse label, a semantic kind (color category), and an optional tap action.\n\nDistinct semantic role from `Component::StatusIndicator`, which today renders screen-body status of in-progress operations (Sync / BackupRecovery / LinkResponder / RecoveryClaimReview / RecoveryHelp use sites). `Indicator` is the chrome-positioned counterpart — emitted by AppEngine overlays (offline / update / future sync-chrome) for app-level status that lives across screens, not as screen content.\n\nPer the shell-purity investigation (`_private/docs/investigations/2026-05-28-core-screen-composition-surface.md`): the variant is generic, not Sync-specific — same shape carries connectivity / backup-overdue / update-available / sync-state uses. Frontends render natively (iOS toolbar chip, GTK4 header status icon, Android Material chip) by typed dispatch, no action_id sniffing.",
          "type": "object",
          "required": [
            "Indicator"
          ],
          "properties": {
            "Indicator": {
              "type": "object",
              "required": [
                "id",
                "kind",
                "label"
              ],
              "properties": {
                "a11y": {
                  "default": null,
                  "anyOf": [
                    {
                      "$ref": "#/definitions/A11y"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "action_id": {
                  "description": "Optional tap action. `None` = display-only (informational); `Some(id)` = tap fires `UserAction::ActionPressed { action_id: id }`.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "id": {
                  "type": "string"
                },
                "kind": {
                  "$ref": "#/definitions/IndicatorKind"
                },
                "label": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "Sectioned action list — multiple labeled groups of tappable items, each rendered as a native section (SwiftUI `Section`, GTK4 ListBox group, Material category header). Distinct semantic role from flat `Component::ActionList`: ignoring the section grouping degrades UX from \"structured menu\" to \"flat dump\", so the discriminant belongs at variant level.\n\nUsed by `MoreEngine` to emit grouped settings entries (primary / secondary / data / legal) without forcing each frontend's renderer to special-case action_ids or reproduce a section table.",
          "type": "object",
          "required": [
            "SectionedActionList"
          ],
          "properties": {
            "SectionedActionList": {
              "type": "object",
              "required": [
                "id",
                "sections"
              ],
              "properties": {
                "id": {
                  "type": "string"
                },
                "sections": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/Section"
                  }
                }
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "ContactActionKind": {
      "description": "Classification of the mutation requested by [`ActionResult::ContactAction`]. Mirrors [`crate::ui::ListItemActionKind`] but lives on the result side so the app layer can dispatch without re-parsing action ids.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "archive",
            "unarchive",
            "hide",
            "unhide",
            "undelete"
          ]
        },
        {
          "description": "Soft-delete (imported contacts only). The inverse is `Undelete`.",
          "type": "string",
          "enum": [
            "delete"
          ]
        }
      ]
    },
    "ContextBar": {
      "type": "object",
      "properties": {
        "back": {
          "anyOf": [
            {
              "$ref": "#/definitions/ActionSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "navigation": {
          "anyOf": [
            {
              "$ref": "#/definitions/ActionSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "primary": {
          "anyOf": [
            {
              "$ref": "#/definitions/ActionSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "secondary": {
          "anyOf": [
            {
              "$ref": "#/definitions/ActionSpec"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "DesignTokens": {
      "description": "Design tokens for consistent cross-platform rendering.\n\nProvides spacing, typography, border radius, touch target, and motion values that all platform clients use for layout consistency. Source of truth: themes/tokens.json. Default impl generated by themes/scripts/generate.py.",
      "type": "object",
      "required": [
        "border_radius",
        "motion",
        "spacing",
        "spacing_direction",
        "touch_target",
        "typography"
      ],
      "properties": {
        "border_radius": {
          "$ref": "#/definitions/BorderRadiusTokens"
        },
        "focus": {
          "default": {
            "ring_offset": 2,
            "ring_width": 3
          },
          "allOf": [
            {
              "$ref": "#/definitions/FocusTokens"
            }
          ]
        },
        "font_family": {
          "default": {
            "body": "Hanken Grotesk",
            "display": "Bricolage Grotesque",
            "mono": "JetBrains Mono"
          },
          "allOf": [
            {
              "$ref": "#/definitions/FontFamilyTokens"
            }
          ]
        },
        "font_weight": {
          "default": {
            "bold": 700,
            "extrabold": 800,
            "medium": 500,
            "regular": 400,
            "semibold": 600
          },
          "allOf": [
            {
              "$ref": "#/definitions/FontWeightTokens"
            }
          ]
        },
        "motion": {
          "$ref": "#/definitions/MotionTokens"
        },
        "spacing": {
          "$ref": "#/definitions/SpacingTokens"
        },
        "spacing_direction": {
          "$ref": "#/definitions/SpacingDirectionTokens"
        },
        "touch_target": {
          "$ref": "#/definitions/TouchTargetTokens"
        },
        "typography": {
          "$ref": "#/definitions/TypographyTokens"
        }
      }
    },
    "DeviceLinkRole": {
      "description": "Role the frontend should assume when entering the device-link flow.",
      "oneOf": [
        {
          "description": "This device creates the link invitation (device-management \"Link New Device\" / the link-initiator UI).",
          "type": "string",
          "enum": [
            "initiator"
          ]
        },
        {
          "description": "This device joins an existing link by scanning an invitation (onboarding or device-replacement \"I have my old device\").",
          "type": "string",
          "enum": [
            "responder"
          ]
        }
      ]
    },
    "DropdownOption": {
      "description": "An option in a [`Component::Dropdown`].",
      "type": "object",
      "required": [
        "id",
        "label"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "label": {
          "type": "string"
        }
      }
    },
    "ExchangeMode": {
      "description": "All supported contact-exchange modes.",
      "oneOf": [
        {
          "description": "Scan a one-sided QR code, then BLE transfers the card — a quick \"glance\" at one peer's QR to bootstrap a BLE connection (the other peer shows the QR). Config and dispatch follow this ritual since the G3 graduation. Open gap: the handshake pins peer identity on a 2-byte advertisement prefix instead of the full QR-pinned identity — see `designs/2026-07-24-exchange-mode-contracts-design.md` (private).",
          "type": "string",
          "enum": [
            "glance"
          ]
        },
        {
          "description": "Bidirectional multi-stage QR + ultrasonic audio proximity confirmation.",
          "type": "string",
          "enum": [
            "hover"
          ]
        },
        {
          "description": "BLE + physical impact detection.",
          "type": "string",
          "enum": [
            "bump"
          ]
        },
        {
          "description": "BLE + accelerometer shake gesture.",
          "type": "string",
          "enum": [
            "shake"
          ]
        },
        {
          "description": "BLE + ambient audio fingerprint match.",
          "type": "string",
          "enum": [
            "magic"
          ]
        },
        {
          "description": "Pure NFC tap exchange — data over NFC (`DataTransport::Nfc`, `NfcBootstrap`), no BLE. Dispatched to `NfcExchangeFlow` via `start_taptap_mode`. (Previously documented as \"BLE bootstrapped via NFC tap\" — stale; the implementation is NFC end to end.)",
          "type": "string",
          "enum": [
            "tap_tap"
          ]
        },
        {
          "description": "Multi-stage QR exchange (like `Hover`) plus the accelerometer shake signal. The composite NFC+BLE+audio+accel ritual is deferred until the single-factor modes pass in isolation (owner decision 2026-07-20; dispatched via `StartMultiStageExchange`).",
          "type": "string",
          "enum": [
            "tap_hover_shake"
          ]
        },
        {
          "description": "Async remote exchange via a shareable URL.",
          "type": "string",
          "enum": [
            "link"
          ]
        },
        {
          "description": "USB cable exchange between a desktop and a phone.",
          "type": "string",
          "enum": [
            "cable"
          ]
        }
      ]
    },
    "ExportFileSpec": {
      "type": "object",
      "required": [
        "data",
        "mime_type",
        "suggested_name"
      ],
      "properties": {
        "data": {
          "type": "array",
          "items": {
            "type": "integer",
            "format": "uint8",
            "minimum": 0.0
          }
        },
        "mime_type": {
          "type": "string"
        },
        "suggested_name": {
          "type": "string"
        }
      }
    },
    "Field": {
      "description": "A contact field as displayed in the UI.\n\n`icon` carries a platform-neutral icon vocabulary name (see [`icon_for_field_type`]) computed by core from `field_type`. Frontends render this directly instead of duplicating the `field_type` → icon switch in each renderer (ADR-021/043 Humble UI). Five frontends previously carried the same switch (iOS×2, cli, tui, android); shipping `icon` on the wire collapses that duplication and makes adding a new field type a single-file change in core.",
      "type": "object",
      "required": [
        "field_type",
        "id",
        "label",
        "value",
        "visibility"
      ],
      "properties": {
        "a11y": {
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/A11y"
            },
            {
              "type": "null"
            }
          ]
        },
        "field_type": {
          "type": "string"
        },
        "icon": {
          "description": "Platform-neutral icon name (see [`icon_for_field_type`]). Frontends map this to their native icon system (SF Symbols / Material Symbols / their preferred glyph table).",
          "default": "",
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "value": {
          "type": "string"
        },
        "visibility": {
          "$ref": "#/definitions/UiFieldVisibility"
        },
        "visibility_label": {
          "description": "Core-resolved localized display copy for `visibility` (see [`visibility_label`]). Frontends render this verbatim; the `visibility` discriminant selects only native color.",
          "default": "",
          "type": "string"
        }
      }
    },
    "FilePickPurpose": {
      "description": "Why a file picker is being opened — lets frontends label the dialog (e.g., \"Import Contacts\" vs \"Import Backup\") without hardcoded strings.\n\nVariants map 1:1 to a label key in the locale store. `Other` covers future imports (e.g., key-bundle import) without forcing every consumer to update for a new well-known purpose.",
      "oneOf": [
        {
          "description": "vCard / VCF import on top of the contacts engine.",
          "type": "string",
          "enum": [
            "ImportContacts"
          ]
        },
        {
          "description": "Encrypted vauchi backup blob.",
          "type": "string",
          "enum": [
            "ImportBackup"
          ]
        },
        {
          "description": "Reserved for future imports — frontends look up `label_key` in the locale store.",
          "type": "object",
          "required": [
            "Other"
          ],
          "properties": {
            "Other": {
              "type": "object",
              "required": [
                "label_key"
              ],
              "properties": {
                "label_key": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "FocusTokens": {
      "description": "Focus-visible ring metrics (the ring colour is a Vauchi/Semantic token).",
      "type": "object",
      "required": [
        "ring_offset",
        "ring_width"
      ],
      "properties": {
        "ring_offset": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "ring_width": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        }
      }
    },
    "FontFamilyTokens": {
      "description": "Font family names (bundled SIL-OFL webfonts; the renderer maps them to platform font handles).",
      "type": "object",
      "required": [
        "body",
        "display",
        "mono"
      ],
      "properties": {
        "body": {
          "type": "string"
        },
        "display": {
          "type": "string"
        },
        "mono": {
          "type": "string"
        }
      }
    },
    "FontWeightTokens": {
      "description": "Font weight ramp.",
      "type": "object",
      "required": [
        "bold",
        "extrabold",
        "medium",
        "regular",
        "semibold"
      ],
      "properties": {
        "bold": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "extrabold": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "medium": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "regular": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "semibold": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        }
      }
    },
    "HapticPattern": {
      "description": "Haptic intent for [`Command::Celebrate`]. Closed enum — platforms map to native patterns (e.g. `UINotificationFeedbackGenerator.success`).",
      "oneOf": [
        {
          "description": "The platform's \"success\" notification haptic.",
          "type": "string",
          "enum": [
            "success"
          ]
        },
        {
          "description": "A single light tap.",
          "type": "string",
          "enum": [
            "light"
          ]
        },
        {
          "description": "No haptic.",
          "type": "string",
          "enum": [
            "none"
          ]
        }
      ]
    },
    "IndicatorKind": {
      "description": "Semantic kind for `Component::Indicator` — the four-state color category each frontend maps to its theme palette.\n\nDistinct from `Status` (used by `Component::StatusIndicator` for in-progress screen-body operations). `IndicatorKind` is for ongoing-state chrome and uses presentation-shaped categories (semantic color roles), not domain-shaped lifecycle states.",
      "oneOf": [
        {
          "description": "In-progress or freshly-confirmed — emphasis color (e.g. green / accent).",
          "type": "string",
          "enum": [
            "Active"
          ]
        },
        {
          "description": "Failed / attention-required — error color (e.g. red / orange).",
          "type": "string",
          "enum": [
            "Error"
          ]
        },
        {
          "description": "Idle / informational — muted color (e.g. gray / outline).",
          "type": "string",
          "enum": [
            "Neutral"
          ]
        },
        {
          "description": "Transient busy state — animated indicator (e.g. spinner / pulse).",
          "type": "string",
          "enum": [
            "Busy"
          ]
        }
      ]
    },
    "InfoItem": {
      "description": "An item in an info panel.",
      "type": "object",
      "required": [
        "detail",
        "title"
      ],
      "properties": {
        "detail": {
          "type": "string"
        },
        "icon": {
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "type": "string"
        }
      }
    },
    "InputType": {
      "description": "Input field type hint for frontends.",
      "type": "string",
      "enum": [
        "Text",
        "Phone",
        "Email",
        "Password"
      ]
    },
    "Item": {
      "description": "A lightweight item shown in `Component::List`. UI-shaped: the renderer doesn't know what kind of thing it represents.",
      "type": "object",
      "required": [
        "id",
        "initials",
        "name"
      ],
      "properties": {
        "a11y": {
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/A11y"
            },
            {
              "type": "null"
            }
          ]
        },
        "actions": {
          "description": "Declarative per-row actions (swipe/long-press/context-menu on mobile, overflow menu on desktop). Empty = no per-row actions. The engine that produced this item chooses which actions make sense given the contact's state (e.g. `Unhide` only on hidden contacts).",
          "default": [],
          "type": "array",
          "items": {
            "$ref": "#/definitions/ListItemAction"
          }
        },
        "id": {
          "type": "string"
        },
        "initials": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "status": {
          "type": [
            "string",
            "null"
          ]
        },
        "subtitle": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "ListItemAction": {
      "description": "A per-row action on a list component. Rendered as swipe on iOS/Android, as overflow menu on desktop. Sent back via [`crate::ui::UserAction::ListItemAction`] when invoked.",
      "type": "object",
      "required": [
        "id",
        "kind",
        "label"
      ],
      "properties": {
        "destructive": {
          "description": "True for permanently-destructive ops that must route through an `InlineConfirm` per ADR-022. Reversible ops (Archive, Hide, soft-Delete) should leave this `false` and rely on toast+undo.",
          "default": false,
          "type": "boolean"
        },
        "id": {
          "description": "Stable identifier echoed back in [`crate::ui::UserAction::ListItemAction`].",
          "type": "string"
        },
        "kind": {
          "description": "Semantic hint driving icon choice and confirmation affordances.",
          "allOf": [
            {
              "$ref": "#/definitions/ListItemActionKind"
            }
          ]
        },
        "label": {
          "description": "Localized label. Frontends may prefer the `kind`-implied icon + localized string keyed on `kind` for swipe UX.",
          "type": "string"
        }
      }
    },
    "ListItemActionKind": {
      "description": "Semantic classification of a [`ListItemAction`]. Frontends map this to the appropriate icon and optional confirmation flow.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "unarchive",
            "unhide",
            "undelete"
          ]
        },
        {
          "description": "Reversible — move contact to the archive (exchanged contacts).",
          "type": "string",
          "enum": [
            "archive"
          ]
        },
        {
          "description": "Reversible — hide contact from the main list.",
          "type": "string",
          "enum": [
            "hide"
          ]
        },
        {
          "description": "Reversible via soft-delete (imported contacts only).",
          "type": "string",
          "enum": [
            "delete"
          ]
        },
        {
          "description": "Escape hatch for new kinds before they get a dedicated variant.",
          "type": "string",
          "enum": [
            "custom"
          ]
        }
      ]
    },
    "MotionTokens": {
      "description": "Motion/animation duration tokens in milliseconds.",
      "type": "object",
      "required": [
        "emphasis_duration_ms",
        "enter_duration_ms",
        "exit_duration_ms"
      ],
      "properties": {
        "emphasis_duration_ms": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "enter_duration_ms": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "exit_duration_ms": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        }
      }
    },
    "NativeWrapperHint": {
      "description": "Hint telling the native shell which hardware-wrapper flow hosts this screen. Replaces frontend-side `screen_id` substring checks used to decide whether to render a `CoreScreenView` or a dedicated native exchange wrapper (`2026-07-06-mobile-domain-shell-violations` I5/A2).",
      "oneOf": [
        {
          "description": "No native wrapper; render through the standard core screen renderer.",
          "type": "string",
          "enum": [
            "None"
          ]
        },
        {
          "description": "The multi-stage bilateral QR/camera exchange wrapper.",
          "type": "string",
          "enum": [
            "MultiStageExchange"
          ]
        },
        {
          "description": "The NFC tap-to-exchange wrapper.",
          "type": "string",
          "enum": [
            "NfcExchange"
          ]
        }
      ]
    },
    "NotificationCategory": {
      "description": "Notification categories that produce OS notifications.",
      "oneOf": [
        {
          "description": "Always fires, no toggle.",
          "type": "string",
          "enum": [
            "EmergencyAlert"
          ]
        },
        {
          "description": "Always fires, no toggle. Distinct from `EmergencyAlert` so the recipient can respond to a coerced sender appropriately (the sender entered their duress PIN — 2026-07-04-coercion-safety-alerts-never-received).",
          "type": "string",
          "enum": [
            "DuressAlert"
          ]
        },
        {
          "description": "Opt-in, off by default.",
          "type": "string",
          "enum": [
            "ContactAdded"
          ]
        },
        {
          "description": "A contact updated their card. The product's core heartbeat (\"Anna updated her number\"). Default **on** (M4 S3, 2026-07-03-notifications-never-authorized).",
          "type": "string",
          "enum": [
            "CardUpdate"
          ]
        }
      ]
    },
    "NotificationPriority": {
      "description": "Presentation urgency for an OS notification.",
      "type": "string",
      "enum": [
        "Default",
        "High",
        "Urgent"
      ]
    },
    "NotificationSpec": {
      "type": "object",
      "required": [
        "body",
        "category_id",
        "category_options",
        "channel_id",
        "title",
        "urgency"
      ],
      "properties": {
        "body": {
          "type": "string"
        },
        "category_id": {
          "type": "string"
        },
        "category_options": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "channel_id": {
          "type": "string"
        },
        "deep_link_uri": {
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "type": "string"
        },
        "urgency": {
          "$ref": "#/definitions/NotificationUrgency"
        }
      }
    },
    "NotificationUrgency": {
      "type": "string",
      "enum": [
        "default",
        "high",
        "urgent"
      ]
    },
    "Orientation": {
      "description": "Screen orientation a frontend should pin to via [`Command::SetOrientationLock`]. Values mirror the platform-native vocabulary (`Activity.requestedOrientation` on Android, `UIInterfaceOrientationMask` on iOS) but are platform-neutral — each frontend maps to its OS API.",
      "oneOf": [
        {
          "description": "Portrait, top of the device pointing up.",
          "type": "string",
          "enum": [
            "Portrait"
          ]
        },
        {
          "description": "Landscape, with no preference for left or right rotation. Most platforms map this to \"user-rotatable landscape\" (the device can rotate between left and right freely while staying landscape-locked).",
          "type": "string",
          "enum": [
            "Landscape"
          ]
        }
      ]
    },
    "OverlayKind": {
      "type": "string",
      "enum": [
        "navigation",
        "action_menu"
      ]
    },
    "OverlaySpec": {
      "type": "object",
      "required": [
        "items",
        "kind"
      ],
      "properties": {
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ActionSpec"
          }
        },
        "kind": {
          "$ref": "#/definitions/OverlayKind"
        },
        "title": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "PaneLayout": {
      "type": "string",
      "enum": [
        "single",
        "split"
      ]
    },
    "PendingNotification": {
      "description": "An OS notification for the frontend to render.",
      "type": "object",
      "required": [
        "body",
        "category",
        "contact_id",
        "event_key",
        "os_category_id",
        "os_category_options",
        "os_channel_id",
        "priority",
        "title"
      ],
      "properties": {
        "body": {
          "type": "string"
        },
        "category": {
          "$ref": "#/definitions/NotificationCategory"
        },
        "contact_id": {
          "type": "string"
        },
        "deep_link_uri": {
          "description": "Deep-link URI the frontend should open when the user taps the notification. Core owns the URI format (ADR-021); the shell forwards it verbatim via `UserAction::LinkOpened`.",
          "type": [
            "string",
            "null"
          ]
        },
        "event_key": {
          "type": "string"
        },
        "os_category_id": {
          "description": "Opaque OS category identifier (iOS/macOS `UNNotificationCategory` identifier). Core mints the id; the shell must not interpret it as a domain concept (ADR-043 Am6).",
          "type": "string"
        },
        "os_category_options": {
          "description": "Opaque tokens that control OS-specific category options. The shell maps each token to the corresponding platform option.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "os_channel_id": {
          "description": "Opaque OS channel identifier (Android `NotificationChannel` id). Core mints the id; the shell must not interpret it as a domain concept (ADR-043 Am6).",
          "type": "string"
        },
        "priority": {
          "description": "Presentation urgency. The shell maps this to OS-specific priority / importance hints; it must not switch on the notification category.",
          "allOf": [
            {
              "$ref": "#/definitions/NotificationPriority"
            }
          ]
        },
        "title": {
          "type": "string"
        }
      }
    },
    "PostOnboardingDestination": {
      "description": "Where to navigate after onboarding completes.",
      "type": "string",
      "enum": [
        "MainScreen",
        "Exchange",
        "ImportContacts",
        "SecurityInfo",
        "BackupSetup"
      ]
    },
    "PresentationAxis": {
      "type": "string",
      "enum": [
        "horizontal",
        "vertical"
      ]
    },
    "PresentationImageShape": {
      "type": "string",
      "enum": [
        "natural",
        "circle"
      ]
    },
    "PresentationInputKind": {
      "type": "string",
      "enum": [
        "text",
        "email",
        "phone",
        "url",
        "password",
        "number",
        "search",
        "pin"
      ]
    },
    "PresentationNode": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "Divider"
          ]
        },
        {
          "type": "object",
          "required": [
            "Text"
          ],
          "properties": {
            "Text": {
              "type": "object",
              "required": [
                "accessibility",
                "content",
                "style"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "content": {
                  "type": "string"
                },
                "id": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "style": {
                  "$ref": "#/definitions/PresentationTextStyle"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Input"
          ],
          "properties": {
            "Input": {
              "type": "object",
              "required": [
                "accessibility",
                "binding_id",
                "enabled",
                "input_kind",
                "label",
                "value"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "binding_id": {
                  "type": "string"
                },
                "enabled": {
                  "type": "boolean"
                },
                "input_kind": {
                  "$ref": "#/definitions/PresentationInputKind"
                },
                "label": {
                  "type": "string"
                },
                "max_length": {
                  "type": [
                    "integer",
                    "null"
                  ],
                  "format": "uint",
                  "minimum": 0.0
                },
                "placeholder": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "validation_error": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "value": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Toggle"
          ],
          "properties": {
            "Toggle": {
              "type": "object",
              "required": [
                "accessibility",
                "binding_id",
                "enabled",
                "label",
                "value"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "binding_id": {
                  "type": "string"
                },
                "enabled": {
                  "type": "boolean"
                },
                "label": {
                  "type": "string"
                },
                "value": {
                  "type": "boolean"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Choice"
          ],
          "properties": {
            "Choice": {
              "type": "object",
              "required": [
                "accessibility",
                "binding_id",
                "enabled",
                "label",
                "options"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "binding_id": {
                  "type": "string"
                },
                "enabled": {
                  "type": "boolean"
                },
                "label": {
                  "type": "string"
                },
                "options": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/ChoiceOption"
                  }
                },
                "selected": {
                  "type": [
                    "string",
                    "null"
                  ]
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Group"
          ],
          "properties": {
            "Group": {
              "type": "object",
              "required": [
                "accessibility",
                "axis",
                "children"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "axis": {
                  "$ref": "#/definitions/PresentationAxis"
                },
                "children": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/PresentationNode"
                  }
                },
                "id": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "label": {
                  "type": [
                    "string",
                    "null"
                  ]
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "List"
          ],
          "properties": {
            "List": {
              "type": "object",
              "required": [
                "accessibility",
                "id",
                "rows",
                "searchable"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "id": {
                  "type": "string"
                },
                "label": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "paging": {
                  "anyOf": [
                    {
                      "$ref": "#/definitions/PresentationPaging"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "rows": {
                  "type": "array",
                  "items": {
                    "$ref": "#/definitions/PresentationRow"
                  }
                },
                "searchable": {
                  "type": "boolean"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Image"
          ],
          "properties": {
            "Image": {
              "type": "object",
              "required": [
                "accessibility",
                "brightness",
                "shape"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "activation": {
                  "anyOf": [
                    {
                      "$ref": "#/definitions/ActionSpec"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "brightness": {
                  "type": "number",
                  "format": "float"
                },
                "data": {
                  "type": [
                    "array",
                    "null"
                  ],
                  "items": {
                    "type": "integer",
                    "format": "uint8",
                    "minimum": 0.0
                  }
                },
                "fallback_text": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "id": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "shape": {
                  "$ref": "#/definitions/PresentationImageShape"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Status"
          ],
          "properties": {
            "Status": {
              "type": "object",
              "required": [
                "accessibility",
                "title",
                "tone"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "activation": {
                  "anyOf": [
                    {
                      "$ref": "#/definitions/ActionSpec"
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "badge": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "detail": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "icon_token": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "id": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "title": {
                  "type": "string"
                },
                "tone": {
                  "$ref": "#/definitions/PresentationTone"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Qr"
          ],
          "properties": {
            "Qr": {
              "type": "object",
              "required": [
                "accessibility",
                "id",
                "payloads",
                "purpose"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "id": {
                  "type": "string"
                },
                "label": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "payloads": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "purpose": {
                  "$ref": "#/definitions/PresentationQrPurpose"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Confirmation"
          ],
          "properties": {
            "Confirmation": {
              "type": "object",
              "required": [
                "accessibility",
                "cancel",
                "confirm",
                "id",
                "warning"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "cancel": {
                  "$ref": "#/definitions/ActionSpec"
                },
                "confirm": {
                  "$ref": "#/definitions/ActionSpec"
                },
                "id": {
                  "type": "string"
                },
                "warning": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Slider"
          ],
          "properties": {
            "Slider": {
              "type": "object",
              "required": [
                "accessibility",
                "binding_id",
                "label",
                "maximum",
                "minimum",
                "value"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "binding_id": {
                  "type": "string"
                },
                "label": {
                  "type": "string"
                },
                "maximum": {
                  "type": "number",
                  "format": "double"
                },
                "maximum_icon": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "minimum": {
                  "type": "number",
                  "format": "double"
                },
                "minimum_icon": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "step": {
                  "type": [
                    "number",
                    "null"
                  ],
                  "format": "double"
                },
                "value": {
                  "type": "number",
                  "format": "double"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Progress"
          ],
          "properties": {
            "Progress": {
              "type": "object",
              "required": [
                "accessibility"
              ],
              "properties": {
                "accessibility": {
                  "$ref": "#/definitions/AccessibilitySpec"
                },
                "label": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "value": {
                  "type": [
                    "number",
                    "null"
                  ],
                  "format": "double"
                }
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "PresentationPaging": {
      "type": "object",
      "required": [
        "offset",
        "total_count",
        "window"
      ],
      "properties": {
        "offset": {
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "total_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        },
        "window": {
          "type": "integer",
          "format": "uint",
          "minimum": 0.0
        }
      }
    },
    "PresentationProfile": {
      "type": "object",
      "required": [
        "active_surface",
        "pane_layout",
        "primary_surface",
        "window_class"
      ],
      "properties": {
        "active_surface": {
          "type": "string"
        },
        "detail_surface": {
          "type": [
            "string",
            "null"
          ]
        },
        "pane_layout": {
          "$ref": "#/definitions/PaneLayout"
        },
        "primary_surface": {
          "type": "string"
        },
        "window_class": {
          "$ref": "#/definitions/WindowClass"
        }
      }
    },
    "PresentationQrPurpose": {
      "type": "string",
      "enum": [
        "display",
        "capture"
      ]
    },
    "PresentationRow": {
      "type": "object",
      "required": [
        "accessibility",
        "controls",
        "enabled",
        "secondary_actions",
        "selected",
        "title"
      ],
      "properties": {
        "accessibility": {
          "$ref": "#/definitions/AccessibilitySpec"
        },
        "activation": {
          "anyOf": [
            {
              "$ref": "#/definitions/ActionSpec"
            },
            {
              "type": "null"
            }
          ]
        },
        "controls": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PresentationNode"
          }
        },
        "detail": {
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "type": "boolean"
        },
        "fallback_text": {
          "type": [
            "string",
            "null"
          ]
        },
        "icon_token": {
          "type": [
            "string",
            "null"
          ]
        },
        "image_data": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "integer",
            "format": "uint8",
            "minimum": 0.0
          }
        },
        "secondary_actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ActionSpec"
          }
        },
        "selected": {
          "type": "boolean"
        },
        "subtitle": {
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "type": "string"
        }
      }
    },
    "PresentationTextStyle": {
      "type": "string",
      "enum": [
        "heading",
        "body",
        "caption",
        "monospace",
        "muted"
      ]
    },
    "PresentationTokens": {
      "type": "object",
      "required": [
        "corner_radius",
        "minimum_target_size",
        "spacing_large",
        "spacing_medium",
        "spacing_small"
      ],
      "properties": {
        "corner_radius": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "minimum_target_size": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "spacing_large": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "spacing_medium": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "spacing_small": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        }
      }
    },
    "PresentationTone": {
      "type": "string",
      "enum": [
        "neutral",
        "accent",
        "success",
        "warning",
        "error"
      ]
    },
    "PreviewVariant": {
      "description": "One alternate look at a `Component::Preview` — a per-variant view of the same content. Today only contact-card group views populate this; future variants (per-locale views, accessibility variants, previews-as-other-relationship) reuse the same shape.\n\nEngines populate `variant_id` with whatever stable identifier they know (group name, locale code, etc.); the renderer only matches it against `Component::Preview.selected_variant` and never reads the string for meaning.",
      "type": "object",
      "required": [
        "display_name",
        "variant_id",
        "visible_fields"
      ],
      "properties": {
        "display_name": {
          "type": "string"
        },
        "variant_id": {
          "type": "string"
        },
        "visible_fields": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Field"
          }
        }
      }
    },
    "Progress": {
      "description": "Step progress indicator.",
      "type": "object",
      "required": [
        "current_step",
        "total_steps"
      ],
      "properties": {
        "current_step": {
          "type": "integer",
          "format": "uint8",
          "minimum": 0.0
        },
        "label": {
          "type": [
            "string",
            "null"
          ]
        },
        "total_steps": {
          "type": "integer",
          "format": "uint8",
          "minimum": 0.0
        }
      }
    },
    "QrMode": {
      "description": "QR code display mode.",
      "type": "string",
      "enum": [
        "Display",
        "Scan"
      ]
    },
    "ScanQuality": {
      "description": "Real-time scan quality indicator for QR camera viewfinder.\n\nFrontends render this as a colored border/frame around the camera preview to guide the user's device positioning: - `Good` → green frame (QR reliably detected) - `Weak` → yellow frame (QR detected but low confidence) - `Poor` → orange frame (intermittent detection) - `NoSignal` → red frame (nothing detected / wrong pointing)",
      "type": "string",
      "enum": [
        "Good",
        "Weak",
        "Poor",
        "NoSignal"
      ]
    },
    "ScreenLayout": {
      "description": "Whether the renderer wraps content in a scroll container or renders a fixed, non-scrolling layout sized to the viewport. Frontends honour this instead of always wrapping content in a scroll view. `Fixed` is used by screens that must not reflow while a live element updates — e.g. the QR exchange screen, where a moving QR breaks the peer camera lock (`2026-06-03-exchange-qr-scan-stability`). `Pinned` is used by list-dominant screens: chrome stays pinned while the list component owns scrolling lazily — eager rendering of 10k rows froze and crashed the mobile renderers (`2026-06-11-contacts-list-windowing-design`).",
      "oneOf": [
        {
          "description": "Content may exceed the viewport; renderer wraps it in a scroll container (the default for list / detail screens).",
          "type": "string",
          "enum": [
            "Scroll"
          ]
        },
        {
          "description": "Content is sized to the viewport and must not scroll or reflow.",
          "type": "string",
          "enum": [
            "Fixed"
          ]
        },
        {
          "description": "Chrome is sized to the viewport and does not scroll; the screen's list component is the scroll host (and, unlike `Fixed`, overlays such as sync chrome may still reflow it).",
          "type": "string",
          "enum": [
            "Pinned"
          ]
        }
      ]
    },
    "ScreenModel": {
      "description": "Describes a full screen to render.",
      "type": "object",
      "required": [
        "components",
        "screen_id",
        "title"
      ],
      "properties": {
        "components": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Component"
          }
        },
        "deprecated_components": {
          "description": "Component types scheduled for removal. Frontends rendering these should display a migration hint in debug builds. Empty unless a deprecation cycle is in progress (2 major versions before removal).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "layout": {
          "description": "Whether the renderer scrolls the content or renders a fixed, non-scrolling layout. See [`ScreenLayout`]. Omitted on the wire when `Scroll` (the default) so only fixed-layout screens carry it.",
          "allOf": [
            {
              "$ref": "#/definitions/ScreenLayout"
            }
          ]
        },
        "native_wrapper_hint": {
          "description": "Which native hardware wrapper should host this screen, if any. `None` means render through the standard core screen renderer. Core owns the decision; frontends stop matching domain `screen_id`s to decide between `CoreScreenView` and dedicated exchange wrappers (`2026-07-06-mobile-domain-shell-violations` I5/A2).",
          "allOf": [
            {
              "$ref": "#/definitions/NativeWrapperHint"
            }
          ]
        },
        "nav_tab_id": {
          "description": "The bottom-nav tab that owns this screen (`my_info` / `contacts` / `exchange` / `groups` / `more`), or `None` for pre-auth / transient screens that show no bottom nav. Unlike [`Self::parent_screen_id`] (which stops at the immediate parent for the desktop sidebar), this resolves transitively to one of the five tab roots so the mobile / TUI 5-tab shells highlight the active tab without re-deriving it from the screen id (retires the hand-maintained `match AppScreen -> tab`).",
          "type": [
            "string",
            "null"
          ]
        },
        "parent_screen_id": {
          "description": "Sidebar/tab the screen belongs to, when it is a sub-screen of a top-level tab (e.g. `contact_detail` belongs to `contacts`). `None` for top-level tabs and transient/global screens (lock, onboarding). Replaces frontend-side `MapScreenToParentId`-style switch statements (`2026-05-01-screen-id-metadata-in-core` G1).",
          "type": [
            "string",
            "null"
          ]
        },
        "presentation_kind": {
          "description": "How to present the screen. See [`ScreenPresentationKind`]; G2 of `2026-05-01-screen-id-metadata-in-core`.",
          "default": "Page",
          "allOf": [
            {
              "$ref": "#/definitions/ScreenPresentationKind"
            }
          ]
        },
        "progress": {
          "anyOf": [
            {
              "$ref": "#/definitions/Progress"
            },
            {
              "type": "null"
            }
          ]
        },
        "schema_version": {
          "description": "Schema version — shells ignore components from higher versions.",
          "default": 5,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "screen_id": {
          "type": "string"
        },
        "subtitle": {
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "type": "string"
        },
        "tokens": {
          "description": "Resolved design tokens for layout consistency. Frontends read spacing, radius, typography from here — never hardcode values.",
          "default": {
            "border_radius": {
              "card": 20,
              "chip": 12,
              "lg": 16,
              "md": 8,
              "md_lg": 12,
              "sheet": 28,
              "sm": 4
            },
            "focus": {
              "ring_offset": 2,
              "ring_width": 3
            },
            "font_family": {
              "body": "Hanken Grotesk",
              "display": "Bricolage Grotesque",
              "mono": "JetBrains Mono"
            },
            "font_weight": {
              "bold": 700,
              "extrabold": 800,
              "medium": 500,
              "regular": 400,
              "semibold": 600
            },
            "motion": {
              "emphasis_duration_ms": 300,
              "enter_duration_ms": 200,
              "exit_duration_ms": 150
            },
            "spacing": {
              "lg": 24,
              "md": 16,
              "sm": 8,
              "sm_md": 12,
              "xl": 32,
              "xs": 4
            },
            "spacing_direction": {
              "content_end": 16,
              "content_start": 16,
              "list_item_end": 8,
              "list_item_inline_end": 12,
              "list_item_inline_start": 12,
              "list_item_start": 8
            },
            "touch_target": {
              "minimum": 44
            },
            "typography": {
              "body_line": 24,
              "body_size": 16,
              "caption_line": 20,
              "caption_size": 14,
              "caption_sm": 12,
              "display": 32,
              "medium_line": 28,
              "medium_size": 20,
              "subtitle_line": 24,
              "subtitle_size": 18,
              "text_scale_percent": 100,
              "title_lg": 20,
              "title_line": 30,
              "title_size": 24
            }
          },
          "allOf": [
            {
              "$ref": "#/definitions/DesignTokens"
            }
          ]
        }
      }
    },
    "ScreenPresentationKind": {
      "description": "How the frontend should present this screen. Replaces frontend-side substring checks on `screen_id` (e.g. windows `screen_id == \"form_dialog\"`, macOS `screen_id.hasPrefix(\"link_\")`).\n\nPer `2026-05-01-screen-id-metadata-in-core` G2 — the renderer routes on the discriminant; it doesn't read the screen-id strings.",
      "oneOf": [
        {
          "description": "Standard pushed/replaced page in the navigation stack.",
          "type": "string",
          "enum": [
            "Page"
          ]
        },
        {
          "description": "Modal dialog stacked over the page (form dialogs, confirmations). Tab switches must dismiss before navigating away.",
          "type": "string",
          "enum": [
            "Modal"
          ]
        },
        {
          "description": "Bottom-sheet / system-sheet style presentation. Pre-empts the iOS / macOS native sheet semantics that today live entirely in the frontend.",
          "type": "string",
          "enum": [
            "Sheet"
          ]
        }
      ]
    },
    "Section": {
      "description": "A named section within a `Component::SectionedActionList`.",
      "type": "object",
      "required": [
        "id",
        "items",
        "label"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ActionListItem"
          }
        },
        "label": {
          "type": "string"
        }
      }
    },
    "SettingsItem": {
      "description": "An item in a settings group.",
      "type": "object",
      "required": [
        "id",
        "kind",
        "label"
      ],
      "properties": {
        "a11y": {
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/A11y"
            },
            {
              "type": "null"
            }
          ]
        },
        "id": {
          "type": "string"
        },
        "info_key": {
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "$ref": "#/definitions/SettingsItemKind"
        },
        "label": {
          "type": "string"
        }
      }
    },
    "SettingsItemKind": {
      "description": "The kind of a settings item.",
      "oneOf": [
        {
          "type": "object",
          "required": [
            "Toggle"
          ],
          "properties": {
            "Toggle": {
              "type": "object",
              "required": [
                "enabled"
              ],
              "properties": {
                "enabled": {
                  "type": "boolean"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Value"
          ],
          "properties": {
            "Value": {
              "type": "object",
              "required": [
                "value"
              ],
              "properties": {
                "value": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Link"
          ],
          "properties": {
            "Link": {
              "type": "object",
              "properties": {
                "detail": {
                  "type": [
                    "string",
                    "null"
                  ]
                }
              }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "Destructive"
          ],
          "properties": {
            "Destructive": {
              "type": "object",
              "required": [
                "label"
              ],
              "properties": {
                "label": {
                  "type": "string"
                }
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "SoundToken": {
      "description": "Sound intent for [`Command::Celebrate`]. Closed enum — platforms map to a bundled asset; respecting the ringer is the frontend's call.",
      "oneOf": [
        {
          "description": "The short exchange chime (\"clinking glasses\").",
          "type": "string",
          "enum": [
            "exchange_chime"
          ]
        },
        {
          "description": "No sound.",
          "type": "string",
          "enum": [
            "none"
          ]
        }
      ]
    },
    "SpacingDirectionTokens": {
      "description": "Directional spacing tokens for content and list items.",
      "type": "object",
      "required": [
        "content_end",
        "content_start",
        "list_item_end",
        "list_item_start"
      ],
      "properties": {
        "content_end": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "content_start": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "list_item_end": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "list_item_inline_end": {
          "default": 12,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "list_item_inline_start": {
          "default": 12,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "list_item_start": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        }
      }
    },
    "SpacingTokens": {
      "description": "Spacing scale for margins, padding, and gaps.",
      "type": "object",
      "required": [
        "lg",
        "md",
        "sm",
        "xl",
        "xs"
      ],
      "properties": {
        "lg": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "md": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "sm": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "sm_md": {
          "default": 12,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "xl": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "xs": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        }
      }
    },
    "StandardShortcut": {
      "type": "string",
      "enum": [
        "back",
        "activate_primary",
        "undo"
      ]
    },
    "Status": {
      "description": "Status for a status indicator component.",
      "type": "string",
      "enum": [
        "Pending",
        "InProgress",
        "Success",
        "Failed",
        "Warning"
      ]
    },
    "SurfaceLayout": {
      "type": "string",
      "enum": [
        "scroll",
        "fixed",
        "pinned"
      ]
    },
    "SurfaceSpec": {
      "type": "object",
      "required": [
        "accessibility_label",
        "layout",
        "nodes",
        "revision",
        "surface_id",
        "title",
        "tokens"
      ],
      "properties": {
        "accessibility_label": {
          "type": "string"
        },
        "layout": {
          "$ref": "#/definitions/SurfaceLayout"
        },
        "nodes": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PresentationNode"
          }
        },
        "revision": {
          "description": "Monotonic Core-owned replacement generation for this surface.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "subtitle": {
          "type": [
            "string",
            "null"
          ]
        },
        "surface_id": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "tokens": {
          "$ref": "#/definitions/PresentationTokens"
        }
      }
    },
    "TextStyle": {
      "description": "Text rendering style.",
      "type": "string",
      "enum": [
        "Title",
        "Subtitle",
        "Body",
        "Caption"
      ]
    },
    "ToastSpec": {
      "type": "object",
      "required": [
        "message"
      ],
      "properties": {
        "message": {
          "type": "string"
        }
      }
    },
    "ToggleItem": {
      "description": "A toggleable item in a list.",
      "type": "object",
      "required": [
        "id",
        "label",
        "selected"
      ],
      "properties": {
        "a11y": {
          "default": null,
          "anyOf": [
            {
              "$ref": "#/definitions/A11y"
            },
            {
              "type": "null"
            }
          ]
        },
        "id": {
          "type": "string"
        },
        "info_key": {
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "type": "string"
        },
        "selected": {
          "type": "boolean"
        },
        "subtitle": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "TouchTargetTokens": {
      "description": "Touch target size tokens for accessibility.",
      "type": "object",
      "required": [
        "minimum"
      ],
      "properties": {
        "minimum": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        }
      }
    },
    "TypographyTokens": {
      "description": "Font size tokens for text hierarchy.",
      "type": "object",
      "required": [
        "body_size",
        "caption_size",
        "subtitle_size",
        "title_size"
      ],
      "properties": {
        "body_line": {
          "default": 24,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "body_size": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "caption_line": {
          "default": 20,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "caption_size": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "caption_sm": {
          "default": 12,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "display": {
          "default": 32,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "medium_line": {
          "default": 28,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "medium_size": {
          "default": 20,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "subtitle_line": {
          "default": 24,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "subtitle_size": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "text_scale_percent": {
          "default": 100,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "title_lg": {
          "default": 20,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "title_line": {
          "default": 30,
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "title_size": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        }
      }
    },
    "UiFieldVisibility": {
      "description": "UI-level field visibility state.\n\nNamed `UiFieldVisibility` to distinguish from `contact::FieldVisibility` which is the storage-level visibility model.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "Shown",
            "Hidden"
          ]
        },
        {
          "type": "object",
          "required": [
            "Scopes"
          ],
          "properties": {
            "Scopes": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "VisibilityMode": {
      "description": "How field visibility is controlled in the UI.",
      "oneOf": [
        {
          "description": "No visibility column — display fields read-only.",
          "type": "string",
          "enum": [
            "ReadOnly"
          ]
        },
        {
          "description": "Show/hide toggle per field.",
          "type": "string",
          "enum": [
            "ShowHide"
          ]
        },
        {
          "description": "Per-group visibility controls.",
          "type": "string",
          "enum": [
            "PerGroup"
          ]
        }
      ]
    },
    "WindowClass": {
      "type": "string",
      "enum": [
        "compact",
        "medium",
        "expanded"
      ]
    }
  }
}